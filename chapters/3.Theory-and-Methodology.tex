\chapter{Theory and methodology}
\label{chap:theory_method}

\begin{center} % NEEDS TO BE WRITTEN AT THE END
\singlespacing\parbox{12cm}{\textit{
This chapter sets out the theory and the methodological choices behind the CFD work of this thesis. The first part establishes the governing equations, the cylindrical-coordinate description of the flow, the velocity triangles and the swirl metrics. It then develops the cascade theory used for the guide vanes, the turbulence-model assumptions and the rotor-stator interaction framework that underlies the two transient analyses. The second part applies this framework to the present geometry. It documents how the design-input flow angle $\alpha_{\mathrm{base}}(r)$ is constructed from a stand-alone transient RDT-only simulation, summarises the parametric MATLAB tool from the project work~\cite{Leonsen2025}, the 18 steady simulations of the design matrix and the no-GV reference simulation. It then describes the multi-domain CFX setup, the mesh strategy, the Grid Convergence Index study and the spectral-analysis methodology used for the two transient runs.
}}
\end{center}

\paragraph{Note on continuity with the project work.}
This thesis builds directly on the project work~\cite{Leonsen2025}. The parametric MATLAB tool \texttt{vanedesign\_loft\_export}, the slip-correction framework and the NACA-based geometric construction (Sections~\ref{sec:theory_airfoil}, \ref{sec:theory_cascade}, \ref{sec:theory_geometric_construction}) are inherited and summarised here, not re-derived.

% PART I: THEORY
% =====================================================================

% =====================================================================
\section*{Part I: Theoretical framework}
\addcontentsline{toc}{section}{Part I: Theoretical framework}
% =====================================================================
\section{Flow fundamentals and turbomachinery kinematics}
\label{sec:theory_kinematics}

The flow between the rim-driven thruster (RDT) and the reversible pump-turbine (RPT) is treated as an incompressible turbulent flow and is modelled in the present work using a Reynolds-averaged formulation. For steady single-phase flow of water, the governing equations are the continuity equation and the Reynolds-averaged momentum equation. In compact vector form,
\begin{equation}
\nabla \cdot \mathbf{u} = 0,
\label{eq:continuity}
\end{equation}
\begin{equation}
\rho \left( \mathbf{u} \cdot \nabla \mathbf{u} \right)
=
-\nabla p
+ \mu \nabla^2 \mathbf{u}
- \rho \nabla \cdot \overline{\mathbf{u}'\mathbf{u}'}.
\label{eq:rans_momentum}
\end{equation}
Here, $\mathbf{u}$ is the mean velocity vector, $p$ is the mean static pressure, $\rho$ is the fluid density, $\mu$ is the dynamic viscosity, and $-\rho \overline{\mathbf{u}'\mathbf{u}'}$ is the Reynolds-stress tensor representing the effect of unresolved turbulent fluctuations. In a Reynolds-averaged Navier-Stokes framework these stresses must be closed through a turbulence model. This formulation suits the present study: the objective is to compare mean-flow quantities such as total-pressure loss, swirl reduction and downstream flow quality, not to resolve the full unsteady turbulent spectrum.

A cylindrical coordinate system fits the geometry because the duct, the RDT wake and the guide-vane annulus are all organised around a common centreline. The flow is therefore described in cylindrical coordinates $(r,\theta,z)$, where $z$ is the axial direction from the RDT towards the RPT, $r$ is the radial coordinate and $\theta$ the circumferential coordinate. The absolute velocity vector $\vec{C}$ is written as
\begin{equation}
\vec{C}(r,\theta,z)
=
C_r(r,\theta,z)\,\hat{e}_r
+
C_\theta(r,\theta,z)\,\hat{e}_\theta
+
C_z(r,\theta,z)\,\hat{e}_z.
\label{eq:cylindrical_velocity}
\end{equation}

\paragraph{Notation.}
In the CFD post-processing the velocity components are often reported as $U_x$ and $U_t$. In the cascade and turbomachinery notation used for the guide-vane design the same components are written as meridional and tangential absolute velocity, with
\begin{equation}
C_m \equiv U_x \equiv C_z,
\qquad
C_u \equiv U_t \equiv C_\theta.
\label{eq:notation_equivalence}
\end{equation}
Throughout this thesis the symbols $C_m$ and $C_u$ are used as the preferred notation for the mean axial and tangential velocity components. Software-specific labels such as \texttt{Velocity.Axial}, \texttt{Velocity.Circumferential}, $U_x$ and $U_t$ are only used when referring explicitly to CFD monitor expressions or post-processing variables.

Downstream of the RDT the flow resembles a confined swirling pipe flow. Experimental and analytical studies of such flows show that the radial velocity is often small compared with the axial and tangential components. The swirl also decays gradually downstream under the action of wall friction and turbulent transport~\cite{Kitoh1991}. For this reason, the RDT outlet and the guide-vane design inlet are therefore described with the approximation
\begin{equation}
C_r \approx 0,
\label{eq:Cr_zero}
\end{equation}
so that the flow is primarily characterised by the axial and tangential components,
\begin{equation}
C_m \equiv C_z, \qquad C_u \equiv C_\theta.
\label{eq:Cm_Cu_def}
\end{equation}
This is a design-level simplification, not a description of the resolved flow. It serves the reduction of the RDT wake to spanwise profiles $C_m(r)$ and $C_u(r)$ for guide-vane design but does not assume that the full flow is axisymmetric. Once the flow enters the vane passage, individual vanes introduce blockage, turning, wakes and end-wall effects that vary azimuthally. The conical section and the $90^\circ$ bend add further asymmetry. The CFD problem is therefore three-dimensional even when the design input is one-dimensional in $r$.

In turbomachinery it is convenient to describe the flow using velocity triangles that relate the absolute velocity $\vec{C}$, the relative velocity $\vec{W}$ seen by a rotating blade row, and the blade speed $\vec{u}$. The relative velocity is
\begin{equation}
\vec{W} = \vec{C} - \vec{u},
\label{eq:relative_velocity}
\end{equation}
where the local blade speed at radius $r$ is $u(r) = \omega r$, with $\omega$ the angular velocity of the rotor. This distinction between absolute and relative velocity is fundamental in rotor rows such as the RDT or the RPT runner, where blade loading is naturally interpreted in the rotating frame. For the guide vanes considered in the present thesis the situation is simpler because the vane row is stationary, so the absolute and relative frames coincide for the guide-vane domain itself. The relative-velocity notation nevertheless remains useful because it provides a direct connection to classical cascade quantities such as inlet angle, outlet angle, metal angle, incidence and deviation.

The local kinematics of the flow are represented by velocity triangles. At each radius the incoming and outgoing velocities are decomposed into axial and tangential components, and the corresponding flow angles are defined relative to the axial direction. The absolute flow angle at the guide-vane inlet is
\begin{equation}
\alpha_2(r) = \tan^{-1}\!\left(\frac{C_{u2}(r)}{C_{m2}(r)}\right).
\label{eq:alpha_inlet}
\end{equation}
The corresponding metal angles at the leading and trailing edges are denoted $\theta_1(r)$ and $\theta_2(r)$, and the stagger angle $\zeta(r)$ defines the orientation of the chord line relative to the axial direction. The relative flow angles upstream and downstream of the vane are denoted $\beta_1(r)$ and $\beta_2(r)$ respectively. The incidence angle at the leading edge is
\begin{equation}
i(r) = \beta_1(r) - \theta_1(r),
\label{eq:incidence}
\end{equation}
A design requirement is to keep $i$ small to avoid leading-edge separation and the associated increase in losses.

\paragraph{Choice of flow angle for stationary guide-vane design.}
The guide vanes are stationary, so the relevant inflow at the leading edge is the absolute angle $\alpha(r)$ defined in~\eqref{eq:alpha_definition}. The local blade speed is zero on the stationary vane, $\vec{W}\equiv \vec{C}$, and the absolute and relative frames coincide. The relative angle $\beta(r)=\arctan(W_u/C_m)$ is reported in Section~\ref{sec:methods_rdt_velocity_triangles} only as a kinematic descriptor of the upstream rotor wake on the blade surface; it is not used in the GV design. The upstream RDT design~\cite{DjupeslandTrivedi2025} is formulated in the rotor-relative frame and uses $\beta$. The guide-vane generator of Leonsen~\cite{Leonsen2025} expects spanwise $(C_m,\,C_u)$ profiles and computes the absolute angle internally as $\alpha = \arctan(C_u/C_m)$. The two angle conventions are consistent once the appropriate frame of reference is identified.

% ---------------------------------------------------------------------
\section{Hydrofoil geometry}
\label{sec:theory_airfoil}
 
The guide vanes are built on NACA-type symmetric sections with a prescribed thickness ratio and a rounded leading edge, stacked along the span and wrapped on a cylindrical or conical surface. Each section is described by its chord length $c$, leading edge (LE), trailing edge (TE), camber line $y_c(x)$ and a thickness law $t(x)$, as sketched in Figure~\ref{fig:airfoil_geometry}. The two-dimensional sectional coefficients $C_L$, $C_D$ and $C_{p,\min}$ are not used as ranking quantities in this thesis, so their definitions are omitted here. The detailed geometric construction (camber, thickness law, stagger, wrapping) is inherited from the project work~\cite{Leonsen2025} and is summarised in Section~\ref{sec:theory_geometric_construction}.
 
\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\linewidth]{Figures/Foil2d/Airfoil_basics.png}
    \caption[NACA 00xx geometrical parameters]{NACA 00xx geometrical parameters used as the base for the guide-vane sections. Adapted from~\cite{Abbott1959}.}
    \label{fig:airfoil_geometry}
\end{figure}
 
% ---------------------------------------------------------------------
\section{Swirl metrics and critical phenomena}
\label{sec:theory_swirl_metrics_def}
 
Quantification of the swirling motion downstream of the RDT and downstream of the guide vanes is one of the central tasks of the post-processing in this thesis. The primary quantity is the non-dimensional swirl number $S$. A standard definition expresses it as the ratio between the axial flux of angular momentum and the axial flux of axial momentum times a characteristic radius~\cite{Gupta1984, Vignat2022}:
\begin{equation}
S(z) \;=\; \frac{1}{R}\,\frac{G_\theta(z)}{G_z(z)},
\label{eq:S_def}
\end{equation}
where for an axisymmetric duct of radius $R$
\begin{align}
G_\theta(z) \;&=\; 2\pi\!\int_0^R \rho\, r^2 \, U_\theta(r,z)\,U_z(r,z)\,\mathrm{d}r,
\label{eq:Gtheta}\\
G_z(z) \;&=\; 2\pi\!\int_0^R \rho\, r \, U_z^{2}(r,z)\,\mathrm{d}r.
\label{eq:Gz}
\end{align}

The guide vanes are designed to reduce the angular-momentum content introduced by the RDT. The swirl number \(S\) is used as the main angular-momentum metric and is evaluated at the analysis planes defined in Section~\ref{sec:methods_planes_metrics}. The final Pareto ranking in Chapter~\ref{chap:results_discussion}, however, uses the mass-flow-averaged residual flow angle \(|\alpha|_{\mathrm{ConeEnd}}\) as the primary RPT-inlet quality metric, while \(S\) is used to interpret the streamwise swirl development.
 
\paragraph{Alternative swirl-number definitions.}
Equation~\ref{eq:S_def} is the form most often used in the combustion literature and is the form adopted as the primary metric in this thesis. The swirl number is not unique. Vignat et al.~\cite{Vignat2022} show that several formally equivalent definitions can give numerical values that differ by tens of percent on non-uniform velocity profiles. The local swirling-strength indicator $\lambda_{ci}$ is the field exported from CFX as \texttt{Velocity.Swirling Strength} and identifies vortex cores from the imaginary part of the eigenvalues of the local velocity-gradient tensor. $\lambda_{ci}$ complements $S$: the scalar swirl number measures the total angular-momentum content of the cross-section, while $\lambda_{ci}$ picks up discrete vortex structures such as tip vortices or precessing cores.
 
\paragraph{Q-criterion for vortex identification.}
A complementary identifier of coherent vortex cores is the second invariant of the velocity-gradient tensor. The velocity-gradient tensor is written as
\begin{equation}
\mathbf{A} = \nabla \vec{u},
\label{eq:vel_grad_tensor}
\end{equation}
and is decomposed into a symmetric strain-rate tensor and an antisymmetric rotation tensor,
\begin{equation}
\mathbf{S} = \frac{1}{2}\left(\mathbf{A}+\mathbf{A}^{T}\right),
\qquad
\boldsymbol{\Omega} = \frac{1}{2}\left(\mathbf{A}-\mathbf{A}^{T}\right).
\label{eq:strain_rotation_tensors}
\end{equation}
The Q-criterion was introduced by Hunt et al.~\cite{Hunt1988} and is defined as
\begin{equation}
Q =
\frac{1}{2}
\left(
\|\boldsymbol{\Omega}\|^{2}
-
\|\mathbf{S}\|^{2}
\right).
\label{eq:Q_def}
\end{equation}
Positive values of \(Q\) indicate regions where local rotation dominates local strain. The criterion is therefore useful for visualising coherent vortex cores, but it is not used as a primary hydraulic ranking metric in this thesis.

For incompressible flow, \(\nabla \cdot \vec{u}=0\), the same quantity can be written as the second invariant of the velocity-gradient tensor,
\begin{equation}
Q
=
-\frac{1}{2}\mathrm{tr}\left(\mathbf{A}^{2}\right).
\label{eq:Q_trace}
\end{equation}
Expanded in Cartesian velocity-gradient components, this is the form evaluated by ANSYS~CFX as \texttt{Velocity.Invariant Q}. 

Since \(Q\) has units of \(\mathrm{s^{-2}}\), its numerical value depends on the velocity and length scales of the flow. For comparison between cases, a fixed reference value is introduced from the bulk velocity at ConeEnd, where the pipe diameter is \(D_{\mathrm{ConeEnd}}=0.4~\mathrm{m}\). The bulk velocity is calculated from the imposed mass flow rate and the cross-sectional area at this plane. With \(\dot{m}=290.3~\mathrm{kg\,s^{-1}}\), water at \(25^\circ\mathrm{C}\) with \(\rho \approx 997~\mathrm{kg\,m^{-3}}\), and
\begin{equation}
A_{\mathrm{ConeEnd}}
=
\frac{\pi D_{\mathrm{ConeEnd}}^2}{4},
\label{eq:Q_coneend_area}
\end{equation}
the bulk velocity becomes
\begin{equation}
U_b =
\frac{\dot{m}}{\rho A_{\mathrm{ConeEnd}}}
=
\frac{\dot{m}}{\rho \pi D_{\mathrm{ConeEnd}}^2/4}
\approx
2.32~\mathrm{m\,s^{-1}}.
\label{eq:Q_bulk_velocity}
\end{equation}
The reference value is then
\begin{equation}
Q_{\mathrm{ref}}
=
\left(\frac{U_b}{D_{\mathrm{ConeEnd}}}\right)^2
\approx
33.6~\mathrm{s^{-2}}.
\label{eq:Q_ref}
\end{equation}
The same \(Q_{\mathrm{ref}}\) is used for all configurations. Regions with \(Q>Q_{\mathrm{ref}}\) are treated as strong vortex-core regions in the post-processing, while regions with \(Q>0\) indicate rotation-dominated flow more generally.

Negative \(Q\) values occur where strain dominates rotation. This is expected in wall-bounded shear layers and is not a numerical artefact. The Q-criterion is therefore interpreted qualitatively together with the velocity field, swirl number and pressure-loss metrics.
 
\paragraph{Critical swirl and vortex breakdown.}
When the swirl ratio exceeds a critical value, swirling pipe flow can develop stagnation zones, wall separation and vortex breakdown~\cite{Rusak2014}. Critical $S$ values reported in the literature cluster around 0.6 for free swirling flows but depend on the specific definition and on the boundary conditions; in confined pipe flow with finite walls the threshold can be higher~\cite{Rusak2014}. Vortex breakdown is associated with large losses and strong unsteadiness, and it is the underlying mechanism behind the rotating vortex rope (RVR) in Francis-turbine draft tubes at off-design~\cite{Brekke2010}. The 0.6 value is not treated as a hard limit here. The flow at the RPT inlet plane is confined and meets a bend rather than a free vortex, so the relevant threshold is higher. The residual swirl number at the RPT inlet plane is interpreted against this value as a qualitative guide rather than a pass-fail criterion.
 
\subsection{Swirl decay in straight pipes}
\label{sec:theory_swirl_decay}
 
In a straight pipe with no swirl-conditioning element, the swirl decays through wall shear and turbulent transport. Experiments by Kitoh~\cite{Kitoh1991} and others show that the decay is approximately exponential and slow relative to typical duct lengths in industrial layouts:
\begin{equation}
S(z) \;\approx\; S(z_0)\,\exp\!\left[\,-\beta\,\frac{z - z_0}{D}\,\right],
\label{eq:S_decay}
\end{equation}
where the decay constant $\beta$ depends weakly on swirl level and Reynolds number and typically takes values $\beta \approx 0.02$-$0.06$ for smooth-walled turbulent flow~\cite{Kitoh1991, Beaubert2016}. The implication is that, for a duct length of a few diameters between the RDT and the RPT, the natural decay alone reduces $S$ only modestly. The decay also adds wall friction, so even the apparent ``benefit'' of letting the swirl decay over a long distance is partly cancelled by additional pressure loss compared with a non-swirling flow at the same Reynolds number. This is why an active swirl-recovery element close to the RDT is preferred over a long downstream pipe as passive damper~\cite{Andersen2014, Nguyen2024}.
 
\paragraph{Implication for the present geometry.}
In the present geometry, the path from the guide-vane trailing-edge plane to the RPT inlet has three sections: a conical contraction of about $0.50$~m, a $90^\circ$ bend, and about $1.0$~m of straight pipe in the final inlet section. This is short compared to the decay length expected from Eq.~\ref{eq:S_decay} for the swirl numbers seen at the guide-vane trailing edge. The residual swirl at the RPT inlet is therefore controlled almost entirely by what the guide vanes deliver and not by free decay in the pipe sections. The post-processing in this thesis quantifies this directly by comparing $S$ at the guide-vane trailing edge with $S$ at the bend exit and at the final inlet plane.
 
% ---------------------------------------------------------------------
\section{Cavitation considerations}
\label{sec:theory_cavitation}
 
Cavitation occurs when the local static pressure $p$ falls to or below the vapour pressure $p_v$. Vapour bubbles form in low-pressure regions and collapse in higher-pressure regions, with possible erosion, noise, vibration and head loss~\cite{Gulich2010}. The available net positive suction head and the Thoma cavitation number,
\begin{equation}
\mathrm{NPSH}_\mathrm{A} \;=\; \frac{p_{\mathrm{in}} - p_v}{\rho g},
\qquad
\sigma \;=\; \frac{\mathrm{NPSH}_\mathrm{A}}{H},
\label{eq:npsha_sigma}
\end{equation}
quantify the cavitation margin, where $p_{\mathrm{in}}$ is the absolute pressure at the machine inlet and $H$ the machine head. Multiphase cavitation modelling is not performed in this thesis. The reference pressure in the present setup was not configured for quantitative cavitation prediction, so the static-pressure field is reported at the cross-section level rather than screened for local $p < p_v$. Quantitative cavitation behaviour of the downstream pump-turbine is covered in the parallel master thesis by Dokset~\cite{Dokset2026}, and full multiphase modelling of a selected configuration is identified as future work in Chapter~\ref{chap:future_work}.
 
% ---------------------------------------------------------------------
% Streamlines figure removed (commented out in source).

\section{Tip-leakage vortex on a hubless cascade}
\label{sec:theory_tip_vortex}
 
In a hubless cascade the inner end of each vane terminates in the open central region. Where the inflow angle is very steep, part of the flow leaks around this inner free end and rolls up into a tip-leakage vortex, driven by the pressure difference between the pressure side and the suction side of the vane~\cite{You2007}. The same mechanism appears on a hubless circular cascade even though the geometry is not the planar two-dimensional cascade of the original reference. The swirling-strength field $\lambda_{ci}$ is a complementary diagnostic for this structure near the inner span. The vortex-core analysis in Chapter~\ref{chap:results_discussion} uses the Q-criterion of Eq.~\eqref{eq:Q_def} as its primary identifier. Figure~\ref{fig:Tip_vortex} sketches the mechanism.
 
\begin{figure}[h!]
  \centering
  \includegraphics[width=0.40\textwidth]{Figures/Theory/Tip_vortex}
  \caption[Tip-leakage vortex mechanism]{Schematic of tip-leakage flow and the formation of a tip-clearance vortex on the inner end of a hubless vane, adapted from You et al.~\cite{You2007}.}
  \label{fig:Tip_vortex}
\end{figure}
 
% ---------------------------------------------------------------------
\section{Cascade theory for guide vanes}
\label{sec:theory_cascade}
 
\subsection{Solidity and pitch}
\label{sec:theory_solidity}
 
At each radius $r$, the guide vanes form an axial cascade with pitch
\begin{equation}
s(r) \;=\; \frac{2\pi r}{Z_s},
\label{eq:pitch}
\end{equation}
and local solidity
\begin{equation}
\sigma(r) \;=\; \frac{c(r)}{s(r)},
\label{eq:solidity}
\end{equation}
where $Z_s$ is the vane count and $c(r)$ the local chord. Solidity controls how much turning the cascade can deliver: high solidity gives more turning per pitch but also more wetted surface, more friction loss and a higher risk of separation between adjacent vanes. A recurring feature of hubless designs is that the local solidity rises sharply at the inner radius because the pitch shrinks with $r$; even a moderate global vane count gives a heavily loaded inner span. This is visible in Figure~\ref{fig:radial_solidity} for the active configurations of this work.
 
\subsection{Diffusion factor and the de Haller criterion}
\label{sec:theory_diffusion}
 
A standard measure of how heavily a blade row is loaded is the Lieblein diffusion factor~\cite{Lieblein1953}:
\begin{equation}
D_f \;=\; 1 - \frac{V_{\mathrm{out}}}{V_{\mathrm{in}}}
       + \frac{\lvert V_{\theta,\mathrm{out}} - V_{\theta,\mathrm{in}} \rvert}
              {2\,\sigma\, V_{\mathrm{in}}},
\label{eq:Df}
\end{equation}
with $V_{\mathrm{in}}$, $V_{\mathrm{out}}$ the inlet and outlet relative velocities, $V_{\theta,\mathrm{in}}$, $V_{\theta,\mathrm{out}}$ the corresponding tangential components, and $\sigma$ the local solidity. Lieblein recommends $D_f \lesssim 0.6$ to keep the suction-side boundary layer attached. A complementary criterion is the de Haller bound on the relative-velocity ratio,
\begin{equation}
\frac{V_{\mathrm{out}}}{V_{\mathrm{in}}} \;\geq\; 0.72,
\label{eq:deHaller}
\end{equation}
which is independent of solidity but cruder than $D_f$. Both criteria are used in the post-processing as descriptive indicators rather than as binary go/no-go limits, because the present application requires turning angles up to $80^\circ$-$90^\circ$ that are well beyond the conventional axial-compressor regime.
 
\subsection{Incidence, deviation and effective turning}
\label{sec:theory_incidence_deviation}
 
Section~\ref{sec:theory_kinematics} introduced the inlet and outlet flow angles $\beta_1(r)$ and $\beta_2(r)$, the metal angles $\theta_1(r)$ and $\theta_2(r)$, the incidence $i(r)$, and the deviation (slip) angle $\delta(r)$. In the cascade view these quantities are used as design constraints:
\begin{itemize}
  \item the leading edge should see a small incidence $i(r)$ at the design point, to avoid early separation and additional loss;
  \item the deviation $\delta(r)$ determines how much of the geometric turning $\theta_1 - \theta_2$ actually appears as flow turning $\beta_1 - \beta_2$;
  \item the effective turning is therefore smaller than the geometric turning, especially for low solidity and high loading.
\end{itemize}
The deviation is treated as a fixed correction $\delta_{\mathrm{TE}} = 6^\circ$ added to the trailing-edge metal angle, following G\"ulich~\cite{Gulich2010}. The project-work response surface~\cite{Leonsen2025} predicts $10^\circ$-$20^\circ$ at the high turning required here. Adopting those values would aggressively over-camber the trailing edge, sharpen the suction-side pressure gradient, and promote separation. The fixed $6^\circ$ correction captures the bulk of the deviation effect without overloading the cascade.

The cost of this choice is that the results report the swirl-reduction performance reachable with a conservative slip correction, not the maximum reachable performance. The same $6^\circ$ correction is applied to all 18 configurations, so it does not bias the comparison between them.
 
% ---------------------------------------------------------------------
\section{Geometric construction of the guide vanes}
\label{sec:theory_geometric_construction}
 
The parametric MATLAB tool \texttt{vanedesign\_loft\_export} of the project work~\cite{Leonsen2025} produces each two-dimensional vane section in five steps. A non-dimensional chordwise coordinate $x^*=x/c\in[0,1]$ is generated with cosine clustering near the leading and trailing edges. A cubic camber line $y_c(x^*)/c = A x^{*3} + B x^{*2} + C x^*$ is fitted so that its end slopes match the LE and TE metal angles $\theta_1(r)$ and $\theta_2(r)$ in a frame aligned with the stagger angle $\zeta(r)$. A modified NACA 00xx symmetric thickness law~\cite{Abbott1959} is applied normal to the camber line, with a leading-edge roundness factor $\texttt{LE\_round}=1.2$ and a finite trailing-edge thickness ratio that gives a fixed relative maximum thickness $(t/c)_{\mathrm{rel}}=0.075$. The two-dimensional sections are then stacked along the span and wrapped on a cylindrical surface of radius $r$. The trailing edges of all sections are anchored to a common azimuth $\phi_{\mathrm{TE,global}}$, which produces a straight TE line in the axial-azimuthal plane and supports a clean axial through-flow when the residual swirl is small. The spanwise lean and sweep parameters exposed by the tool are held at zero in this work so that the chord and solidity factors are isolated; lean and sweep are listed as candidates for a second-stage refinement in Chapter~\ref{chap:future_work}. The detailed expressions and the implementation are documented in~\cite{Leonsen2025}.
 
\subsection{Constant chord versus constant solidity}
\label{sec:theory_const_chord_const_sol}
 
The two design strategies adopted in this work are exposed by the boolean flag \texttt{use\_sigma} in the design tool:
\begin{itemize}
  \item \textbf{Constant chord (CC).} The chord $c$ is a single constant value, identical at every radius. The solidity $\sigma(r) = c\,Z_s/(2\pi r)$ then decreases hyperbolically with radius, so the loading per pitch is concentrated near the inner span and decreases towards the shroud.
  \item \textbf{Constant solidity (CS).} The chord is set as $c(r) = \sigma_{\mathrm{target}}\,s(r)$ with $\sigma_{\mathrm{target}}$ fixed, so the solidity is uniform in radius and the chord scales linearly with $r$. 
\end{itemize}
The two strategies are not equivalent, and the design matrix is constructed precisely to compare them at matched levels of loading. The case-naming convention is defined with the matrix in Section~\ref{sec:methods_design_matrix}.



\begin{figure}[h!]
    \centering
    \fbox{\includegraphics[width=0.95\textwidth]{Figures/Methods/CC_vs_CS_point_cloud.png}}
%DEAR HÅVARD 12.10 (figur, byttet ut - F4 LOEST): Figuren er erstattet med CC_vs_CS_point_cloud.png (fra TIL RESULTS). Bekreftet ved aa aapne bildet: samme innhold (CC500 venstre med overlappende konstant-korde-seksjoner, CS500 hoyre med viftende seksjoner, farge = radius), men aksene og colorbar bruker naa RUNDE parenteser (m) i stedet for [m]. F4-enhetsavviket er dermed lukket for denne figuren. MERK: selve PNG-en maa kopieres inn i Figures/Methods/ (jeg har ikke et binaer-kopi-verktoey mellom diskene i denne oekten); includegraphics peker naa paa CC_vs_CS_point_cloud.png.
    \caption[Point cloud CC vs CS design]{Unwrapped STL point-cloud representation of the CC500 and CS500 guide-vane geometries. The colour indicates the local radius \(r\).}
    \label{fig:compare_cc_cs}
\end{figure}

The geometric difference between the two strategies is illustrated in Figure~\ref{fig:cc_vs_cs_3d} for the matched pair \texttt{CC\_400\_8} and \texttt{CS\_400\_8}. Both designs share the same shroud chord ($c_{\mathrm{shroud}} = 400$~mm) and the same vane count ($Z_s = 8$). The CC vane keeps a constant chord all the way to the inner cut-out at $r = 0.12$~m. The CS vane tapers linearly inwards, so the chord at the hub is reduced to $c_{\mathrm{hub}} = \sigma_{\mathrm{shroud}}\,(2\pi r_{\mathrm{hub}}/Z_s) \approx 191$~mm. Three-dimensional renderings of all 18 active configurations are collected in Appendix~\ref{app:gv_3d_geometries}, and the corresponding two-dimensional section stacks in Appendix~\ref{app:gv_2d_sections}.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \fbox{\includegraphics[width=\textwidth]{Figures/GV_Design/3D/CC_400_8_Helices_Vane_axial_X_.png}}
    \caption{\texttt{CC\_400\_8}: constant chord, $c = 400$~mm.}
    \label{fig:cc_400_8_3d}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \fbox{\includegraphics[width=\textwidth]{Figures/GV_Design/3D/CS_400_8_Helices_Vane_axial_X_.png}}
    \caption{\texttt{CS\_400\_8}: $c_{\mathrm{shroud}} = 400$~mm}
    \label{fig:cs_400_8_3d}
  \end{subfigure}
  \caption[CC and CS guide-vane pair]{Matched \texttt{CC\_400\_8} and \texttt{CS\_400\_8} guide-vane geometries. The full set of 18 configurations is shown in Appendix~\ref{app:gv_3d_geometries}.}
  \label{fig:cc_vs_cs_3d}
\end{figure}
% ---------------------------------------------------------------------
\section{Secondary flow and Dean vortices in 90$^\circ$ bends}
\label{sec:theory_dean_bend}
 
The geometry adopted for the simulations includes a $90^\circ$ bend between the cone and the final inlet section. This element is not passive. When fluid passes through a curved duct, the centripetal acceleration $V^2/R_c$ requires a radial pressure gradient across the cross-section. The velocity, however, is not uniform: it is reduced near the wall by the boundary layer and is largest in the core of the duct. The local centripetal acceleration is therefore not uniform across the section, and the cross-stream pressure gradient cannot balance it everywhere simultaneously. The fluid responds by establishing a pair of counter-rotating secondary cells in the cross-stream plane: the \emph{Dean vortices}~\cite{Dean1927}.
 
\subsection{The Dean number and bend regimes}
\label{sec:theory_dean_number}
 
The strength of the Dean vortices scales with the dimensionless Dean number
\begin{equation}
\mathrm{De} \;=\; \mathrm{Re}\,\sqrt{\frac{D_h}{2\,R_c}},
\label{eq:dean}
\end{equation}
where $D_h$ is the hydraulic diameter and $R_c$ the centreline radius of
curvature. Here $D = 0.4$~m and $R_c = 0.297$~m, so $R_c/D = 0.74$, below
the $R_c/D \approx 1.5$ threshold of Kim et al.~\cite{KimYadavKim2014}
beneath which the bend is sharp enough to separate on the inner radius
rather than form a clean Dean pair. With $\mathrm{Re} \sim 10^6$ the
secondary flow is fully turbulent, dominated by two large counter-rotating
cells that interact with the upstream-imposed swirl. The inner-radius
separation that the sharp curvature implies is treated in
Section~\ref{sec:theory_bend_separation}.
 
\subsection{Interaction with upstream swirl}
\label{sec:theory_swirl_bend_interaction}
 
When the upstream flow already carries swirl, the bend secondary flow is no longer a clean symmetric Dean pair. Experiments on turbulent flow downstream of a \(90^\circ\) bend with imposed swirl show that the Dean vortices are perturbed by the incoming swirl and tend to merge towards a single-cell structure as the swirl level increases~\cite{KalpakliOrlu2013}. The post-bend velocity field is therefore interpreted as a combined result of bend curvature, residual swirl and swirl direction, not as a Dean-vortex field set by the bend alone. Escue and Cui~\cite{EscueCui2010} show that the SST \(k\)-\(\omega\) model captures the bulk structure of swirling pipe flow. The model may still underpredict turbulence anisotropy under strong rotation, which is a limitation when interpreting RANS results in this regime.
 
The implication for the present thesis is that:
\begin{enumerate}
  \item the guide-vane performance must be evaluated not only at the trailing-edge plane (where the design tool has direct geometric control) but also at planes downstream of the bend, because swirl-bend interaction changes the effective ranking;
  \item the sign of the residual swirl is a relevant design parameter even if the design matrix focuses primarily on chord-strategy and vane count;
  \item the radial non-uniformity of the flow at the RPT inlet plane, not just its cross-sectional mean, must be reported; the azimuthal structure left by the bend is carried qualitatively by the cross-section secondary-flow and vortex-core fields rather than quantified per case.
\end{enumerate}
 
\subsection{Inner-radius separation in the bend}
\label{sec:theory_bend_separation}
 
A further bend-related effect is separation on the inner radius of the bend, where the adverse pressure gradient is strongest. Jenssen~\cite{Jenssen2020} reported this effect in the original RPT draft-tube geometry and used it to motivate the placement of a velocity-profile boundary condition downstream of the bend rather than upstream. In the present configuration the same inner-radius separation may occur and is likely to be more severe when the upstream swirl reinforces the inner-side pressure gradient. The post-processing characterises the bend response through the cross-stream secondary-flow field and the in-plane streamlines at a sequence of bend planes, together with the net in-plane swirl and the bend head loss for a set of reference cases (Chapter~\ref{chap:results_discussion}). Resolving the inner-radius wall separation itself, through the surface-streamline pattern and the wall pressure on the inner bend, needs a wall-resolved near-wall mesh and is left to the follow-up identified in Chapter~\ref{chap:future_work}.
 
% ---------------------------------------------------------------------
\section{Turbulence modelling and numerical limitations}
\label{sec:theory_turbulence}
 
The simulations in this thesis use a Reynolds-averaged formulation with the SST $k$-$\omega$ turbulence model of Menter~\cite{Menter1994}. The SST model blends the standard $k$-$\omega$ model in the near-wall region with a $k$-$\varepsilon$ formulation in the outer flow, and is the de-facto standard for hydraulic turbomachinery in industrial CFD. Four known limitations are relevant to the present work and shape how the results are interpreted in Chapter~\ref{chap:results_discussion}. First, two-equation eddy-viscosity models assume an isotropic eddy viscosity and underpredict Reynolds-stress anisotropy under strong rotation and curved streamlines; in swirling pipe flow this gives a slightly diffused tangential-velocity profile compared with experimental data~\cite{EscueCui2010}. Second, the Dean vortices in the $90^\circ$ bend arise from the same streamline-curvature mechanism that the model tends to underpredict, so the bulk Dean structure is captured but absolute secondary-velocity magnitudes and the location of inner-radius separation are mesh- and coefficient-sensitive. Third, transition is not modelled: the chord-Reynolds numbers are high enough that the boundary layers are turbulent everywhere except possibly near the leading edge at the inner span, and the resulting small underestimation of leading-edge loss is in the same direction for all configurations and does not bias the comparative ranking. Fourth, the 18 simulations of the design matrix are run as steady RANS, so unsteadiness from rotor-stator interaction, GV trailing-edge shedding, and any precessing core downstream is not resolved; two separate transient simulations address the rotor-stator question (Sections~\ref{sec:rsi-thesis}-\ref{sec:spec-coherence}), and full transient post-processing of all 18 cases is left as future work.

% ---------------------------------------------------------------------
\section{Rotor-stator interaction and pressure pulsations}
\label{sec:rsi}
 
In the compact RDT-GV-duct configuration considered in this thesis, the rotating RDT blades and the stationary GV ring are separated by a relatively small axial distance. The flow through the GV is therefore not strictly steady, even at the nominal operating point. Two mechanisms produce unsteady loading on the GV and a corresponding pressure pulsation field in the duct~\cite{Brekke2001, Arndt1990}:
\begin{enumerate}
  \item \emph{Potential interaction.} The pressure field attached to each rotor blade convects past the stationary vanes. It produces a pressure fluctuation that decays exponentially with axial distance but can be significant when the axial gap is a fraction of a blade chord.
  \item \emph{Viscous (wake) interaction.} Each rotor blade sheds a velocity-deficit wake. When such a wake is chopped by a stationary vane, the local angle of attack changes momentarily and produces an impulsive change in vane loading.
\end{enumerate}
Both mechanisms are periodic in the rotor reference frame at the rotor frequency $f_n$, and in the stator reference frame at the blade passing frequency $f_{\mathrm{BPF}}$ defined below. They are the main source of deterministic unsteadiness in the present configuration.

\subsection{Axial spacing between the rotor and the stationary vane row}
\label{sec:rsi-axial-spacing}

Rotor-stator interaction strength scales with the axial distance between the rotor trailing edge and the stator leading edge. In compact axial-pump stages this clearance is non-dimensionalised by the impeller chord $L$ as $a/L$, with $a$ the axial gap. G\"ulich~\cite{Gulich2010} recommends a clearance in the range
\begin{equation}
    \frac{a}{L} \;=\; 0.05\,\text{-}\,0.15
    \label{eq:gulich_a_over_L}
\end{equation}
to limit dynamic vane loading and pressure pulsations from rotor-stator interaction. Bijukchhe and Trivedi~\cite{Bijukchhe2026} adopt the same range in their recent design study of axial booster pumps for retrofitted reversible pump-turbines. They add a further recommendation that the impeller-to-stator vane-count ratio satisfies $Z_r/Z_s \approx 0.7$-$0.9$, to weaken harmonic reinforcement at the rotor-stator interface. The ratios in the present work are $Z_r/Z_s = 7/6 \approx 1.17$, $7/8 = 0.875$ and $7/9 \approx 0.78$. The $Z_s = 8$ and $Z_s = 9$ families fall inside or close to the recommended window. The $Z_s = 6$ family lies outside it.

The recommendation~\eqref{eq:gulich_a_over_L} was developed for compact axial-pump stages, in which the diffuser vanes are placed immediately downstream of the impeller. The geometry studied in this thesis differs from this configuration. The hubless RDT discharges into a duct section that connects to the guide-vane row, and the stationary vanes are not placed at the rotor trailing edge. The bound~\eqref{eq:gulich_a_over_L} is therefore retained as a qualitative reference for why the axial spacing matters, rather than as a binding design constraint. The actual axial spacing in the present geometry is documented in Section~\ref{sec:methods_axial_spacing}, and the practical implications for the rotor-stator interaction analysis are discussed in Section~\ref{sec:rsi-thesis} and revisited in the spectral analysis of Chapter~\ref{chap:results_discussion}.

\subsection{Rotor frequency and blade passing frequency}
\label{sec:rsi-bpf}
 
With rotor rotational speed $n$~(rpm), the rotor frequency is
\begin{equation}
    f_n \;=\; \frac{n}{60}. \label{eq:fn}
\end{equation}
A single stationary vane observes $Z_r$ rotor blades passing in each rotor revolution. The fundamental excitation frequency seen by a single GV is therefore the \emph{blade passing frequency}~\cite{Brekke2001}
\begin{equation}
    f_{\mathrm{BPF}} \;=\; Z_r\, f_n. \label{eq:fbpf}
\end{equation}
A single rotor blade sees $Z_s$ stationary vanes per rotor revolution and therefore experiences a periodic loading at the \emph{vane passing frequency}
\begin{equation}
    f_{\mathrm{VPF}} \;=\; Z_s\, f_n. \label{eq:fvpf}
\end{equation}
In the special case $Z_r = Z_s$ the two frequencies coincide, $f_{\mathrm{BPF}} = f_{\mathrm{VPF}}$. Higher harmonics $k f_{\mathrm{BPF}}$, $k=2,3,\dots$ are typically present because the actual pulse shape is not sinusoidal; their amplitude decreases with $k$ but can remain non-negligible for the second and third harmonic if the wake is narrow~\cite{Rodriguez2007}.
 
\subsection{Tyler-Sofrin modal decomposition}
\label{sec:rsi-tyler}
 
The classical analysis by Tyler and Sofrin~\cite{Tyler1962} for an annular rotor-stator stage shows that the unsteady pressure field downstream of the stator can be written as a sum of circumferential modes
\begin{equation}
    p'(r,\theta,z,t) \;=\; \sum_{k=1}^{\infty}\sum_{j=-\infty}^{\infty}
      \hat{p}_{k,j}(r,z)\,
      \exp\!\left[\, i\bigl(m_{k,j}\,\theta - k\,Z_r\,\Omega\,t\bigr)\,\right],
    \label{eq:tyler_sofrin_expansion}
\end{equation}
where $\Omega = 2\pi f_n$ and the circumferential mode order is
\begin{equation}
    m_{k,j} \;=\; k\,Z_r - j\,Z_s, \qquad k,j \in \mathbb{Z}.
    \label{eq:tyler_sofrin}
\end{equation}
Each combination $(k,j)$ corresponds to a pressure pattern that rotates around the axis with phase speed $k\,Z_r\,\Omega/m_{k,j}$ and oscillates at frequency $k f_{\mathrm{BPF}}$ at a fixed point in the stator frame. The mode order $m_{k,j}$ determines how the pulsation couples with the surrounding duct: an $m=0$ pattern is axisymmetric and propagates as a plane wave, whereas patterns with $|m|\geq 1$ are spinning modes whose upstream or downstream propagation depends on whether the frequency is above or below the cut-off frequency of the corresponding duct mode~\cite{Morse1968}.
 
For the fundamental BPF harmonic $k=1$ the lowest-order spatial mode is
\begin{equation}
    m_{\min} \;=\; \min_{j\in\mathbb{Z}} \bigl| Z_r - j\,Z_s \bigr|.
    \label{eq:mmin}
\end{equation}
Three cases are of particular relevance:
\begin{itemize}
  \item If $Z_r = Z_s$, Eq.~\eqref{eq:mmin} gives $m_{\min}=0$ with $k=j=1$. The lowest-order mode at $f_{\mathrm{BPF}}$ is the \emph{axisymmetric breathing mode}: all $Z_s$ vanes are loaded synchronously and in phase. The pressure field is a plane wave that propagates along the duct without a cut-off, and the integrated axial force on the GV ring adds coherently at $f_{\mathrm{BPF}}$.
  \item If $\gcd(Z_r,Z_s)=1$ (coprime), the minimum mode order is at least $|Z_r - Z_s|$ and typically $m_{\min} \geq 1$. The net axial force on the ring approaches zero by cancellation around the circumference. The remaining loading appears as a rotating radial force on the rotor.
  \item If $Z_r = Z_s = 7$, which corresponds to the configuration most directly inherited from the project work and which forms one of the two transient baselines analysed in this thesis, the breathing mode $m=0$ is unavoidable in principle. Whether it actually dominates the pulsation field in practice depends on how closely the true wake pattern of the RDT satisfies the $Z_r$-fold azimuthal symmetry that the Tyler-Sofrin argument assumes.
\end{itemize}
It is therefore an established design guideline in axial turbomachinery to choose $Z_r$ and $Z_s$ so that $\gcd(Z_r,Z_s)=1$, or at least $Z_r \neq Z_s$, which removes the synchronous $m=0$ breathing mode at $f_{\mathrm{BPF}}$~\cite{Tyler1962,Brekke2001}, with equivalent recommendations for radial pump turbines~\cite{Tanaka2011}. For reversible pump-turbines, Binama et al.~\cite{Binama2021FlowInstability} show that runner blade number affects vaneless-space pressure pulsations. This supports treating blade-count choices as system-level design variables, while the present thesis applies that argument to the RDT-GV pair.
 
\subsection{Structural and acoustic resonance}
\label{sec:rsi-resonance}

Unsteady blade forcing becomes critical when an excitation frequency in
Eq.~\eqref{eq:tyler_sofrin_expansion} approaches a structural or acoustic
natural frequency of the system~\cite{Tanaka2011}. The GV
blade operates in water and therefore has a strong added-mass effect.
Reported in-water natural-frequency reduction ratios range from about
$0.8$ in open water to $0.45$-$0.50$ for confined Francis runner modes
with few diametrical nodes~\cite{Tanaka2011}. The GV is a thin foil in a
relatively open passage, so its reduction is expected to lie nearer the
open-water end of that range. Confirming this requires a dedicated modal
analysis of the final GV design that resolves the added-mass and
boundary-condition effects~\cite{Valentin2015}, and this is identified as
future work in Chapter~\ref{chap:future_work}.
 
\subsection{Practical magnitudes and resonance thresholds}
\label{sec:rsi-magnitudes}

The practical significance of an unsteady loading is decided by its amplitude relative to the mean loading. Normalised pressure pulsation amplitudes $\tilde{p}_{E} = A_p / (\rho g H)$ below approximately $1\,\%$ at the blade passing frequency are generally acceptable for continuous operation, while values above $3$-$5\,\%$ usually require design modification~\cite{Brekke2001, Egusquiza2006}. These reference values are used as diagnostic thresholds in the interpretation of the CFD results in Chapter~\ref{chap:results_discussion}.
 
\subsection{Relation to the present thesis}
\label{sec:rsi-thesis}
 
The choice $Z_r = Z_s = 7$ that corresponds to the project-work baseline places the fundamental spatial mode at $m_{\min} = 0$ at the frequency $f_{\mathrm{BPF}} = 7\,f_n$. The theoretical expectation is that this configuration is \emph{a priori} unfavourable compared with configurations using $Z_s \in \{6,8,9\}$, for which $m_{\min} \geq 1$ and the net axial force on the GV ring at $f_{\mathrm{BPF}}$ would cancel to leading order.
 
The present configuration uses $Z_s \in \{6,8,9\}$ and therefore avoids $m_{\min}=0$ throughout. Two transient simulations are run in addition to the 18 steady simulations to test the Tyler-Sofrin argument directly in the present compact RDT-GV geometry. The first uses the original project-work configuration $Z_s = 7$ (case \texttt{CC\_450\_7}, the $m_{\min}=0$ reference). The second uses $Z_s = 6$ (case \texttt{CC\_400\_6}, a representative member of the design matrix with $m_{\min} \geq 1$). The pair therefore brackets the Tyler-Sofrin distinction between the synchronous breathing mode ($Z_r=Z_s$) and the leading spinning mode ($Z_r\neq Z_s$). A diagnostic for the effective weight of the $m=0$ mode in the computed pulsation field is introduced in Section~\ref{sec:spec-coherence}.

\paragraph{Selection of the $m_{\min}\geq 1$ counterpart.}
\texttt{CC\_400\_6} is used as the $m_{\min}\geq 1$ counterpart rather than the geometrically closer \texttt{CC\_450\_6}. The Tyler-Sofrin spatial mode order depends only on $Z_r$ and $Z_s$, not on the shroud chord. The $50$~mm shroud-chord difference changes the local solidity by approximately $11\,\%$, small compared with the $17\,\%$ change in $\sigma$ between $Z_s=6$ and $Z_s=7$ in the same family. \texttt{CC\_400\_6} also sits closer to the centre of the design matrix in solidity space, so the transient diagnostic is informative about the bulk of the 18 steady configurations.

\paragraph{Effect of the large axial spacing.}
A second factor moderates the comparison. The axial spacing is large relative to the compact axial-pump bound ($a/L_{\mathrm{mid}} \approx 1.39$, well above the G\"ulich window; Section~\ref{sec:methods_axial_spacing}), so the potential-interaction component is expected to be weak at the GV row and the $m=0$ breathing-mode signature anticipated for $Z_r = Z_s = 7$ is attenuated relative to the compact-stage textbook case. The compact-stage idealisation behind the Tyler-Sofrin argument is therefore only partly satisfied here, and the transient diagnostic in Chapter~\ref{chap:results_discussion} is interpreted in that context.

% =====================================================================
% PART II: METHODOLOGY
% =====================================================================
\section*{Part II: Numerical method and case setup}
\addcontentsline{toc}{section}{Part II: Numerical method and case setup}
% =====================================================================
\section{Overview of the methodology}
\label{sec:methods_overview_master}
 
The methodology has four elements. The first is a stand-alone transient simulation of the RDT alone, which gives the time-averaged design-input flow angle $\alpha_{\mathrm{base}}(r)$ used by the geometry generator (Section~\ref{sec:methods_design_input_baseline}). The second is the parametric MATLAB tool inherited from the project work~\cite{Leonsen2025}, which uses $\alpha_{\mathrm{base}}(r)$ to generate the 18 candidate guide-vane geometries of the design matrix (Section~\ref{sec:methods_design_matrix}). The third is a steady multi-domain ANSYS~CFX setup that simulates each of the 18 configurations and the \texttt{NoGV} reference in the full RDT-GV-draft-tube layout, under identical operating conditions, and a structured post-processing pipeline that extracts a common set of metrics on a fixed array of analysis planes. The fourth is a pair of transient simulations of two configurations from the matrix (\texttt{CC\_450\_7} and \texttt{CC\_400\_6}) for the rotor-stator interaction analysis described in Section~\ref{sec:rsi-thesis}. The MATLAB tool itself is the same tool developed in the project work~\cite{Leonsen2025}; its operating principle is briefly recalled here and is not redeveloped from scratch. The CFX setup is specific to the present thesis. The draft-tube domain has been extended to include the conical contraction, the $90^\circ$ bend and a final straight inlet pipe. The boundary conditions reflect the full nominal operating point at $\dot m = 290.3$~kg/s and $n = 650$~rpm.
 
Figure~\ref{fig:methodology_overview} summarises the workflow. Each branch of the design matrix (Section~\ref{sec:methods_design_matrix}) goes through the same pipeline. Sections are generated in MATLAB and converted into TurboGrid blade and hub/shroud curve files by an auxiliary MATLAB routine. Blade meshing is done in ANSYS TurboGrid. The blade mesh is then integrated with the structured RDT mesh and the unstructured downstream-duct mesh, simulated in ANSYS~CFX, and post-processed on the common plane array.
 
\begin{figure}[h!]
  \centering
  \includegraphics[width=0.7\textwidth]{Figures/FlowChart/Master_full_workflow.png}
%DEAR HÅVARD 12.10 (figur, tall-avvik): Flytskjemaet (Stage 1) skriver "Spanwise wake at 0.25 m downstream". Men prosaen i sec:methods_design_input_baseline sier at vaken samples paa et plan 0.20 m nedstroms RDT-TE, og bildetekstene til fig:alpha_full_radial og fig:alpha_baseline sier ogsaa 0.20 m. Den interne logikken i teksten (0.20 m sampling, GV-LE ved 0.192 m, 8 mm gap mellom dem) er selvkonsistent paa 0.20 m, saa 0.25 i flytskjemaet ser ut som feilen (muligens forvekslet med shroud-radius 0.25 m). Reconciler: regenerer flytskjemaet med "0.20 m downstream" (eller bekreft hvilket tall som er riktig og rett det andre stedet). Jeg kan ikke redigere PNG-en. Resten av flytskjemaet stemmer med den beskrevne pipelinen.
  \caption[Methodology overview]{Methodology overview for the 18 steady simulations and the two transient diagnostic runs. Each configuration follows the same pipeline from MATLAB section generation to CFX simulation and post-processing.}
  \label{fig:methodology_overview}
\end{figure}
 
% ---------------------------------------------------------------------
\section{Design-input flow angle from a stand-alone RDT simulation}
\label{sec:methods_design_input_baseline}

The parametric MATLAB tool of Section~\ref{sec:methods_matlab_tool} needs a spanwise inflow angle $\alpha(r)$ to set the leading-edge metal angles. This angle is taken from a dedicated transient simulation of the RDT alone, run separately from the 18 simulations of the design matrix. The simulation uses the same operating point ($\dot m = 290.3$~kg/s, $n = 650$~rpm) and the same RDT geometry as the rest of this work, but the GV region is replaced by a straight pipe of equal length. The time step is $\Delta t = 2.564\times 10^{-4}$~s, corresponding to $1^\circ$ of rotor rotation per step. The total simulated time is $2.18$~s, approximately $23.6$ rotor revolutions. The aim is to produce a time-averaged wake field that is converged in the design-relevant radii and that can be used as a deterministic input to the MATLAB tool.

The wake field is sampled on a plane located $0.20$~m downstream of the RDT trailing edge. The GV leading edge in the full domain sits at $0.192$~m downstream of the same reference point (Section~\ref{sec:methods_axial_spacing}). The sampling plane is therefore $8$~mm downstream of the GV leading-edge position in the full domain. This is in the straight unloaded section before any vane influence and is hydraulically equivalent to a sample at the GV leading-edge plane, because there is no flow turning between the two stations. The sampling plane was used in place of the exact GV leading-edge plane because the latter sits close to the rotating-stationary interface where small numerical disturbances from the steady frozen-rotor approximation used in the 18 simulations would contaminate the angle.

This design-input angle is taken from the downstream wake plane, after the wake has mixed over approximately $0.20$~m, not from the blade-surface trailing-edge triangles of Section~\ref{sec:methods_rdt_velocity_triangles} (which include local boundary-layer effects and reach above $80^\circ$ in the outer span).

\paragraph{Inner cut-out from the radial backflow boundary.}
The RDT wake has a low-momentum and reversed-flow region in the centre. Figure~\ref{fig:alpha_full_radial} plots the time-averaged axial and tangential velocity components $U_x(r)$ and $U_t(r)$ across the full pipe radius. The axial component is negative for $r \lesssim 0.12$~m and remains negative throughout the last four converged revolutions. The vane has no positive momentum to operate on inside this radius, so the cascade design cannot be applied there. The inner radius of the GV design span is therefore set at $r = 0.12$~m. This fixes the inner cut-out used by every configuration in the design matrix.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.80\textwidth]{Figures/Methods/BaselinePlot1.png}
%DEAR HÅVARD 12.10 (figur, kvantitet-avvik - VIKTIG): Bildeteksten (kort: "Baseline radial velocity profiles") og prosaen rett over sier at figuren plotter "the time-averaged axial and tangential velocity components U_x(r) and U_t(r)", og teksten hevder "the axial component is negative for r <= 0.12 m". Men det faktiske bildet (BaselinePlot1.png) plotter FLYT-VINKELEN alpha = atan2(|C_u|,|C_m|) i grader (alltid positiv, 0-100 grader) over full radius, med backflow-skygge og stiplede "backflow-dominant rings". Det viser IKKE hastighetskomponentene, og den negative aksialhastigheten (paastanden i teksten) er ikke synlig i et vinkelplott. Dette er baade et figur-tekst-avvik OG en claim-linkage-svakhet (paastanden om negativ U_x mangler stoette i den refererte figuren). To valg: (a) bytt BaselinePlot1.png til et faktisk U_x(r)/U_t(r)-plott som viser U_x < 0 innenfor r<0.12; eller (b) behold vinkelplottet, men skriv om bildeteksten og prosaen til aa beskrive alpha(r) og begrunn cut-out via de stiplede backflow-ringene i stedet for via "negativ U_x". TILLEGG: aksene bruker klammeparenteser [deg] og [m] - skal vaere runde (deg) og (m), samme avvik som fig:compare_cc_cs. Jeg kan ikke redigere PNG-en.
    \caption[Baseline radial velocity profiles]{Radial profiles of the time-averaged axial and tangential velocity components, $U_x(r)$ and $U_t(r)$, sampled $0.20$~m downstream of the RDT trailing edge.}
    \label{fig:alpha_full_radial}
\end{figure}


\paragraph{Construction of $\alpha_{\mathrm{base}}(r)$.}
The flow angle is computed from the sampled velocity field as \[\alpha(r) = \arctan\left(\frac{U_t(r)}{U_x(r)}\right).\] Only the positive-flow region is used for GV design. The resulting design span is $r \in [0.12, 0.25]$~m. A smoothed curve, $\alpha_{\mathrm{base}}(r)$, is used as input to the parametric MATLAB tool.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.80\textwidth]{Figures/Methods/BaselinePlot2.png}
    \caption[GV design-span flow angle]{Raw and smoothed flow angle across the GV design span, with the backflow region removed. The red dashed line is the baseline for GV design}
    \label{fig:alpha_baseline}
\end{figure}

The MATLAB tool builds each vane section from the local inflow angle. A non-monotonic input gives oscillatory section angles that are difficult to manufacture and that distort the comparison between configurations. The raw angle is therefore replaced by a smoothed, monotone baseline $\alpha_{\mathrm{base}}(r)$, built from the average of the last four converged revolutions. Over the inner half of the design span ($r \lesssim 0.18$~m) the baseline follows the averaged angle to within $1^\circ$, where the rev-to-rev scatter is about $1.5^\circ$. On the outer half the baseline is flattened to a constant $60^\circ$ plateau. This plateau deliberately does not chase two features of the raw curve: a mid-span dip to about $52^\circ$ near $r = 0.21$~m, and a steep near-wall rise to about $74^\circ$ at the shroud, both of which are partly transient and partly wall-driven. Relative to the raw averaged angle, the resulting nominal incidence is small on the inner span, reaches about $-7.5^\circ$ in the mid-span dip, and grows to about $+14.8^\circ$ at the shroud.

The same $\alpha_{\mathrm{base}}(r)$ is used to generate every configuration, so a matched CC and CS pair at the same shroud chord and vane count is built from an identical inflow angle at every radius. The smoothing therefore does not bias the comparison within a matched pair. At the family-mean level a small residual remains, because the largest nominal incidence sits at the shroud, where the two families differ most in local chord, so a constant-solidity vane carries more blade length through that offset than a constant-chord vane. This residual is treated as a limitation on the family-level ranking rather than on the matched-pair comparison.

% ---------------------------------------------------------------------
\section{Velocity-triangle characterisation of the RDT wake}
\label{sec:methods_rdt_velocity_triangles}
 
This section characterises the RDT wake through velocity triangles evaluated on the rotor blade trailing edge. These triangles describe the kinematic state of the flow on the blade surface in the TE region. They are reported for completeness and as a check on the upstream RDT design~\cite{DjupeslandTrivedi2025}. They are \emph{not} the input used by the parametric MATLAB tool to design the guide vanes; that input is the design-input flow angle $\alpha_{\mathrm{base}}(r)$ constructed in Section~\ref{sec:methods_design_input_baseline} from the stand-alone transient RDT simulation.
 
\paragraph{Extraction procedure.}
The absolute velocity components $C_m$ and $C_u$ are extracted from the converged \texttt{NoGV} steady solution at six radial spans, $r/R \in \{0.01,\,0.20,\,0.40,\,0.60,\,0.80,\,0.99\}$, with $R_{\mathrm{hub}}=53.95$~mm and $R_{\mathrm{shroud}}=250.0$~mm. The values are sampled along blade-loading lines on the rotor blade at constant span. The streamwise coordinate $s\in[0,1]$ runs from the leading edge ($s=0$) to the trailing edge ($s=1$), and the TE region used here is $s\in(0.90,\,1.00)$.
 
\paragraph{Sign convention.}
The streamwise direction is defined as the direction from the RDT towards the RPT. In the post-processing, exported CFX velocity components are sign-adjusted when needed so that $C_m>0$ corresponds to flow in this streamwise direction and $C_u>0$ corresponds to motion in the rotor direction. For the blade-loading-line exports used here this gives
\begin{equation}
C_m = -V_{\mathrm{axial,CFX}}, \qquad
C_u = -V_{\mathrm{circ,CFX}}, \qquad
U(r)=\omega r,
\label{eq:rdt_sign_convention}
\end{equation}
and the relative tangential component is $W_u=C_u-U$. The radial component $C_r$ is neglected at this stage, in line with Section~\ref{sec:theory_kinematics}.
 
\paragraph{Trailing-edge values.}
A blade-loading line traces the rotor blade contour and contains at every $s$ both a pressure-side and a suction-side branch. The streamwise coordinate is binned into 200 equal intervals; in each bin the mean of the PS and SS branches gives a single representative line. The TE values obtained from $s\in(0.90,\,1.00)$ are summarised in Table~\ref{tab:rdt_te_values} and visualised in Figure~\ref{fig:rdt_velocity_triangles_TE}. The absolute flow angle on the blade surface is
\begin{equation}
\alpha(r) \;=\; \arctan\!\left(\frac{C_u(r)}{C_m(r)}\right).
\label{eq:alpha_definition}
\end{equation}
The values in Table~\ref{tab:rdt_te_values} therefore describe the local kinematics on the blade trailing-edge surface, sampled along the blade-loading line.
 
\begin{table}[H]
  \centering
  \caption[RDT trailing-edge velocity triangles]{Velocity-triangle components sampled on the RDT trailing-edge blade surface at six radial spans.}
  \label{tab:rdt_te_values}
  \begin{tabular}{ccccccccc}
    \hline
    span & $r$ & $U$ & $C_m$ & $C_u$ & $W_u$ & $|\vec{C}|$ & $\alpha$ & $\beta$ \\
         & [mm] & [m/s] & [m/s] & [m/s] & [m/s] & [m/s] & [$^\circ$] & [$^\circ$] \\
    \hline
    0.01 &  55.9 &  3.81 & $-0.03$ &  4.22 & $+0.41$ &  4.22 & $+90.3$ & $+93.5$ \\
    0.20 &  93.2 &  6.34 &  1.00 &  5.21 & $-1.13$ &  5.31 & $+79.1$ & $-48.3$ \\
    0.40 & 132.4 &  9.01 &  1.44 &  6.63 & $-2.38$ &  6.78 & $+77.8$ & $-58.9$ \\
    0.60 & 171.6 & 11.68 &  1.67 &  8.22 & $-3.46$ &  8.39 & $+78.5$ & $-64.2$ \\
    0.80 & 210.8 & 14.35 &  1.67 &  9.98 & $-4.37$ & 10.12 & $+80.5$ & $-69.0$ \\
    0.99 & 248.0 & 16.88 &  0.21 & 15.71 & $-1.17$ & 15.71 & $+89.2$ & $-79.8$ \\
    \hline
  \end{tabular}
\end{table}
 
\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\linewidth]{Figures/RDT/RDT_velocity_triangles_TE.png}
  \caption[RDT trailing-edge velocity triangles]{Velocity triangles at the RDT trailing edge for six radial spans.}
  \label{fig:rdt_velocity_triangles_TE}
\end{figure}
 
\paragraph{Wake structure observed in the trailing-edge region.}
The meridional component $C_m$ starts at $-0.03$~m/s at the innermost sampled span and rises to a peak of $1.67$~m/s in the central span band $r/R\in[0.60,\,0.80]$. It then drops to $0.21$~m/s near the shroud. This C-shaped profile is consistent with the low-momentum inner region and the free-end vortex interaction expected in a hubless rim-driven thruster~\cite{DjupeslandTrivedi2025}. The tangential component $C_u$ increases monotonically with radius from $4.22$~m/s at the innermost sampled span to $15.71$~m/s near the shroud, qualitatively consistent with the forced-vortex distribution targeted in the upstream design~\cite{DjupeslandTrivedi2025}. The absolute flow angle $\alpha$ stays between $78^\circ$ and $90^\circ$ across the sampled RDT trailing-edge region. It is closest to purely tangential at the endpoints ($90.3^\circ$ at the innermost sample and $89.2^\circ$ near the shroud) and reaches its lowest value of $77.8^\circ$ at $r/R = 0.40$. The wake on the blade trailing edge is therefore dominated by tangential motion, which is what the guide vanes are designed to redirect towards the axial direction.
 
\paragraph{Inner cut-out.}
The values for $r/R\in\{0.01,0.20\}$ correspond to radii $r=55.9$~mm and $r=93.2$~mm. Both lie inside the inner cut-out, below the guide-vane design span, which starts at $r=120$~mm and extends to the nominal shroud radius $r=250$~mm. The inner cut-out is fixed by the radial backflow boundary identified in Section~\ref{sec:methods_design_input_baseline}. The inner sampled RDT wake values are therefore reported as wake diagnostics only; they are not used to set the local guide-vane geometry.


\section{Parametric guide-vane design tool}
\label{sec:methods_matlab_tool}
 
The guide-vane geometries are generated by the MATLAB tool \texttt{vanedesign\_loft\_export.m} that was developed in the project work~\cite{Leonsen2025}. The tool reads spanwise distributions $\{r,\,C_m(r),\,C_u(r)\}$ from the RDT wake. From these it computes the local inlet flow angle $\alpha_1(r) = \arctan(C_u/C_m)$ and the local diameter-based Reynolds number $\mathrm{Re}_D(r)$. It then constructs the metal angles, the chord distribution, the two-dimensional camber and thickness, and the three-dimensional wrapped vane surface. The geometric construction is described in Section~\ref{sec:theory_geometric_construction}. The output of the tool is a set of section curves and an STL file. In the present thesis, these are converted directly into TurboGrid \texttt{Blades.curve}, \texttt{Hub.curve} and \texttt{Shroud.curve} input files by an auxiliary MATLAB routine. This replaces the Visual Studio and SolidWorks lofting pipeline used in the project work.
 
The tool exposes a structured options list, of which the following are exercised across the 18 configurations:
\begin{itemize}
  \item \texttt{Zs}: vane count;
  \item \texttt{use\_sigma}: boolean switching between fixed chord (\texttt{false}) and target solidity (\texttt{true});
  \item \texttt{c\_const\_fallback}: shroud chord when \texttt{use\_sigma = false};
  \item \texttt{sigma\_target}: target solidity at the shroud when \texttt{use\_sigma = true};
  \item \texttt{slip\_choice = 'const'} with \texttt{slip\_const\_deg = 6.0}: fixed slip correction in line with G\"ulich's recommendation~\cite{Gulich2010};
  \item \texttt{tc\_rel = 0.075}, \texttt{tTE\_rel = 0.005}, \texttt{LE\_round = 1.2}: thickness law parameters held constant;
  \item \texttt{use\_cone\_wrap = false}, \texttt{use\_TE\_anchor = true}: cylindrical wrapping with a common trailing-edge azimuth;
  \item \texttt{lean\_amp = 0}, \texttt{sweep\_amp = 0}: lean and sweep held at zero across all 18 configurations.
\end{itemize}
 
\paragraph{Choice of slip-correction strategy.}
The project work~\cite{Leonsen2025} developed a CFD-based slip database and a smooth response-surface model that predicts deviation as a function of inlet flow angle and Reynolds number. At the high turning angles required to recover the RDT swirl, this response surface predicts deviation values of $10^\circ$-$20^\circ$. Adopting these large built-in trailing-edge angles over-cambers the section near the trailing edge, sharpens the suction-side adverse pressure gradient, and tends to promote separation. The classical recommendation of G\"ulich~\cite{Gulich2010} is more conservative, with $\delta_{\mathrm{TE}}\approx 4^\circ$-$6^\circ$ for well-designed pump and stator vanes. The upper end of this range, $\delta_{\mathrm{TE}} = 6^\circ$, is applied as a constant correction to the trailing-edge metal angle for every section across all 18 configurations. This isolates the chord-strategy and vane-count effects that the master thesis is designed to study.

\paragraph{Spanwise inflow.}
The spanwise inflow angle $\alpha_{\mathrm{base}}(r)$ used by the tool is constructed from the stand-alone transient RDT simulation described in Section~\ref{sec:methods_design_input_baseline}. The same $\alpha_{\mathrm{base}}(r)$ is used for every configuration of the design matrix, so all 18 designs are generated from the same upstream wake.
 
% ---------------------------------------------------------------------
\section{Design matrix: 18 configurations}
\label{sec:methods_design_matrix}
 
The design space is sampled on a structured matrix of 18 configurations that combines two chord-control strategies, six shroud-chord levels and three vane counts. The matrix is symmetric: every constant-chord (CC) configuration has a constant-solidity (CS) twin at the same shroud chord and vane count, which isolates the effect of the chord-control strategy from the effect of loading. A nineteenth simulation, denoted \texttt{NoGV}, replaces the guide-vane region with an empty straight pipe of equal length and serves as the swirl-reduction reference against which all 18 configurations are compared (Section~\ref{sec:methods_rdt_baseline}).
 
\paragraph{Choice of chord-control strategies.}
Two chord-control strategies are used. In the constant-chord cases (CC), $c(r)$ is fixed across the span, which increases the local solidity towards the hub. In the constant-solidity cases (CS), $c(r)$ scales with radius, giving a more uniform solidity distribution. These two strategies give a simple comparison between high hub solidity and nearly constant spanwise loading.
 
\begin{table}[h!]
  \centering
  \caption[Design matrix]%
  {Design matrix for the 18 GV configurations and the \texttt{NoGV} reference case.}
  \label{tab:design_matrix}
  \small
  \begin{tabular}{l|c|c|c}
    \toprule
    \textbf{Case ID} & \textbf{Strategy} & $c_{\mathrm{shroud}}$ (mm) & $Z_s$ \\
    \midrule
    CC\_250\_9  & Constant chord    & 250 & 9 \\
    CC\_300\_9  & Constant chord    & 300 & 9 \\
    CC\_350\_8  & Constant chord    & 350 & 8 \\
    CC\_400\_6  & Constant chord    & 400 & 6 \\
    CC\_400\_8  & Constant chord    & 400 & 8 \\
    CC\_450\_6  & Constant chord    & 450 & 6 \\
    CC\_450\_8  & Constant chord    & 450 & 8 \\
    CC\_500\_6  & Constant chord    & 500 & 6 \\
    CC\_500\_8  & Constant chord    & 500 & 8 \\
    \midrule
    CS\_250\_9  & Constant solidity & 250 & 9 \\
    CS\_300\_9  & Constant solidity & 300 & 9 \\
    CS\_350\_8  & Constant solidity & 350 & 8 \\
    CS\_400\_6  & Constant solidity & 400 & 6 \\
    CS\_400\_8  & Constant solidity & 400 & 8 \\
    CS\_450\_6  & Constant solidity & 450 & 6 \\
    CS\_450\_8  & Constant solidity & 450 & 8 \\
    CS\_500\_6  & Constant solidity & 500 & 6 \\
    CS\_500\_8  & Constant solidity & 500 & 8 \\
    \midrule
    \texttt{NoGV} & No guide vanes (reference) & - & - \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Mesh distribution across the 18 cases.}
The mesh was kept identical in all non-GV domains across the 18 configurations. The only source of variation in total element count is the geometry-dependent TurboGrid mesh of the GV domain, which changes with chord length and vane count. Table~\ref{tab:mesh-elements-gv-cases} lists the element count per domain for each case. Total mesh size ranges from 25.4 to 31.4 million elements.

\begin{table}[H]
\centering
\small
\caption[Mesh element count per domain]{Number of mesh elements in each computational domain for the 18 guide-vane configurations, in million elements.}
\label{tab:mesh-elements-gv-cases}
\begin{tabular}{lccccccc}
\hline
Case
& GV
& InnerPipeGV
& Inner pipe
& RDT
& Inlet pipe
& DT/cone
& Total \\
\hline
CC\_250\_9 & 6.090 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 25.364 \\
CC\_300\_9 & 7.024 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 26.297 \\
CC\_350\_8 & 7.551 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 26.825 \\
CC\_400\_6 & 10.347 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 29.621 \\
CC\_400\_8 & 10.866 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 30.139 \\
CC\_450\_6 & 8.640 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 27.914 \\
CC\_450\_8 & 9.295 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 28.569 \\
CC\_500\_6 & 11.978 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 31.252 \\
CC\_500\_8 & 9.166 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 28.439 \\
\hline
CS\_250\_9 & 6.582 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 25.856 \\
CS\_300\_9 & 6.971 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 26.245 \\
CS\_350\_8 & 7.969 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 27.243 \\
CS\_400\_6 & 9.315 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 28.589 \\
CS\_400\_8 & 9.861 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 29.135 \\
CS\_450\_6 & 10.292 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 29.566 \\
CS\_450\_8 & 11.795 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 31.069 \\
CS\_500\_6 & 10.644 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 29.918 \\
CS\_500\_8 & 12.143 & 0.357 & 1.249 & 11.965 & 0.746 & 4.956 & 31.417 \\
\hline
\end{tabular}
\end{table}

\paragraph{Coverage.}
The design matrix covers shroud chords from $250$ to $500$~mm and vane counts $Z_s \in \{6,8,9\}$. Each CC case has a matching CS case at the same $(c_{\mathrm{shroud}}, Z_s)$, so the effect of chord-control strategy can be compared directly. The selected cases span dense short-chord and sparse long-chord arrangements while avoiding extreme low- or high-solidity designs.

\paragraph{Constants across the matrix.}
All 18 configurations and the \texttt{NoGV} reference share the same constants, so that the only differences between the GV configurations are the design variables in Table~\ref{tab:design_matrix}. The constants are: thickness ratio $(t/c)_{\mathrm{rel}} = 0.075$; trailing-edge thickness $t_{\mathrm{TE}}/c = 0.005$; leading-edge roundness factor $\texttt{LE\_round} = 1.2$; slip correction $\delta_{\mathrm{TE}} = 6^\circ$; cylindrical wrapping; trailing-edge anchoring at a common azimuth; and design span $r \in [0.12, 0.25]$~m. Operating conditions are held fixed at $\dot m = 290.3$~kg/s, $n = 650$~rpm, water at $25^\circ$C.

\paragraph{Two additional transient runs.}
In addition to the 18 steady configurations of Table~\ref{tab:design_matrix} and the \texttt{NoGV} reference, two extra simulations are run in transient mode for the rotor-stator interaction analysis (Sections~\ref{sec:rsi-thesis} and~\ref{sec:monitor-setup}-\ref{sec:spec-coherence}). The first is \texttt{CC\_450\_7}, which corresponds to the project-work baseline geometry with $Z_r=Z_s=7$ and is the reference for the synchronous Tyler-Sofrin breathing mode ($m_{\min}=0$). The second is \texttt{CC\_400\_6}, used as the $m_{\min}\geq 1$ counterpart from the design matrix; the choice of \texttt{CC\_400\_6} rather than \texttt{CC\_450\_6} is motivated in the closing paragraph of Section~\ref{sec:rsi-thesis}. Both transient runs use the same multi-domain CFX setup, mesh strategy and operating point as the 18 steady simulations, with the rotor-stator interfaces switched from frozen rotor to transient rotor-stator (Section~\ref{sec:methods_bc}, Section~\ref{sec:monitor-setup}).

% ---------------------------------------------------------------------
\section{Design-space visualisation}
\label{sec:methods_design_space_viz}

The design matrix in Table~\ref{tab:design_matrix} is summarised graphically in Figures~\ref{fig:design_space_overview}-\ref{fig:reference_card}. The four plots together show three things. They visualise where the 18 active configurations sit in the loading-chord plane. They show how the constant-chord and constant-solidity strategies differ in radial loading. They compare the cases against the recommended cascade-loading bound $\sigma \lesssim 1.6$, adopted as a soft design guideline in axial-pump and stator design~\cite{Gulich2010}.

Figure~\ref{fig:design_space_overview} plots all configurations in the $(\sigma_{\mathrm{shroud}},\,c_{\mathrm{shroud}})$ plane. The 18 active configurations are marked in colour. Greyed-out points show the corresponding $Z_s = 7$ designs, included for context only. These coincide with the original project-work baseline ($Z_r = Z_s = 7$) and are excluded on Tyler-Sofrin grounds (Section~\ref{sec:rsi-thesis}). The dashed vertical line at $\sigma_{\mathrm{shroud}} = 1.6$ separates the recommended-loading region from the over-loaded region. Only the smallest active configurations (\texttt{CC\_250\_9}, \texttt{CS\_250\_9}, \texttt{CC\_400\_6}, \texttt{CS\_400\_6}) sit close to or inside the recommended region at the shroud; the remainder of the matrix is loaded above this guideline at the shroud.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.95\textwidth]{Figures/GV_Design/Design_plots/fig1_master_design_space.png}
  \caption[Design-space overview]{Design-space overview of the 18 active configurations. Constant-chord (CC) cases are red circles, constant-solidity (CS) cases are blue squares. Greyed points are the inactive $Z_s = 7$ context cases. The dashed vertical line at $\sigma_{\mathrm{shroud}} = 1.6$ separates the recommended region (green) from the above-limit region (red) following the soft cascade-loading guideline of G\"ulich~\cite{Gulich2010}. Most of the matrix is intentionally loaded beyond the conventional limit, because the high turning required to recover the RDT swirl forces the design into this regime.}
  \label{fig:design_space_overview}
\end{figure}

Figure~\ref{fig:hub_vs_shroud} compares the local solidity at the hub of the design span ($r = 0.12$~m) with the local solidity at the shroud ($r = 0.25$~m). For the constant-solidity (CS) family the chord is by construction proportional to $r$, so $\sigma_{\mathrm{hub}} = \sigma_{\mathrm{shroud}}$ and all CS points lie on the diagonal. For the constant-chord (CC) family the same chord is used at every radius, so $\sigma_{\mathrm{hub}} = \sigma_{\mathrm{shroud}}\,(R_{\mathrm{shroud}}/R_{\mathrm{hub}}) \approx 2.08\,\sigma_{\mathrm{shroud}}$, and all CC points lie above the diagonal. The figure quantifies the central design distinction between the two families: a CC vane is more heavily loaded near the hub than near the shroud, while a CS vane has uniform loading along the span. The horizontal dashed line at $\sigma_{\mathrm{hub}} = 1.6$ shows that all CC configurations exceed the recommended hub loading, by a factor of two or more for the largest cases. CS configurations remain within or close to the limit at the hub if and only if they are within the limit at the shroud.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.95\textwidth]{Figures/GV_Design/Design_plots/fig2_hub_vs_shroud.png}
  \caption[Hub vs shroud loading]{Hub solidity $\sigma_{\mathrm{hub}}$ versus shroud solidity $\sigma_{\mathrm{shroud}}$ for the 18 active configurations. CS cases lie on the diagonal $\sigma_{\mathrm{hub}} = \sigma_{\mathrm{shroud}}$. CC cases lie on the steeper line $\sigma_{\mathrm{hub}} \approx 2.08\,\sigma_{\mathrm{shroud}}$ because the same chord is used at every radius. The recommended-loading limit $\sigma \lesssim 1.6$ is exceeded at the hub by every CC configuration in the matrix.}
  \label{fig:hub_vs_shroud}
\end{figure}

Figure~\ref{fig:radial_solidity} traces the local solidity $\sigma(r) = c(r)Z_s/(2\pi r)$ across the design span $r \in [0.12, 0.25]$~m for all chord and vane-count combinations. The CC profiles fall hyperbolically with $r$ (solid lines), while the CS profiles are flat in $r$ (dashed lines) by construction. The figure shows where the high-$\sigma$ regions sit on each design and is the radial-resolved counterpart of the two scalar plots above.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.99\textwidth]{Figures/GV_Design/Design_plots/fig3_radial_solidity.png}
  \caption[Radial solidity profile]{Radial profile of the local solidity $\sigma(r) = c(r)Z_s/(2\pi r)$ across the design span $r \in [0.12, 0.25]$~m for all chord and vane-count combinations. Solid lines: constant-chord (CC) family, with $\sigma$ falling hyperbolically with $r$. Dashed lines: constant-solidity (CS) family, with $\sigma$ uniform in $r$ by construction. The dashed red horizontal line marks the soft loading guideline $\sigma = 1.6$.}
  \label{fig:radial_solidity}
\end{figure}

Figure~\ref{fig:reference_card} sorts the 18 active configurations by hub solidity $\sigma_{\mathrm{hub}}$ in ascending order and lists the chord at the hub $c_{\mathrm{hub}}$, the chord at the shroud $c_{\mathrm{shroud}}$, and the local solidities at hub, mid-span and shroud. Cells are colour-coded against the soft loading guideline (yellow when $\sigma \leq 1.6$, pink when above). The card is used as a quick reference in the discussion of the results in Chapter~\ref{chap:results_discussion}. \texttt{CS\_250\_9} is the only configuration that sits inside the recommended region at every span, with $\sigma = 1.43$ uniform. \texttt{CS\_400\_6} is just inside the limit at $\sigma = 1.52$ uniform. The remaining 16 configurations exceed the recommended loading at the hub. The worst case is \texttt{CC\_500\_8}, which reaches $\sigma_{\mathrm{hub}} = 5.31$.

\begin{figure}[h!]
  \centering
  \includegraphics[width=0.95\textwidth]{Figures/GV_Design/Design_plots/fig4_reference_card.png}
  \caption[Active-design reference card]{Reference card for the active GV designs, sorted by hub solidity. Yellow marks $\sigma \leq 1.6$; pink marks $\sigma > 1.6$.}
  \label{fig:reference_card}
\end{figure}

\paragraph{Implication of the high-$\sigma$ region.}
The diffusion-factor and de Haller bounds are, in this geometry, indicative rather than binding. They are exceeded over part of the span by every active configuration because the very high RDT outflow angles ($\alpha > 80^\circ$ in the outer span; Section~\ref{sec:methods_rdt_velocity_triangles}) require a large flow turning that cannot be delivered with $\sigma \lesssim 1.6$ along the full span. The present work accepts this and uses the bounds as comparative loading indicators, in line with established practice for highly loaded axial machines~\cite{Schnoes2018, Leonsen2025}. The four figures above support the interpretation of the CFD results in Chapter~\ref{chap:results_discussion} by making explicit which configurations are loaded the most aggressively and where on the span this loading is concentrated.

% ---------------------------------------------------------------------
\section{Computational domain and boundary conditions}
\label{sec:methods_domain}
 
The computational domain reproduces the planned Store2Hydro layout from the RDT inlet down to the location where the RPT inlet would be. Six contiguous regions are connected through stationary-stationary or rotating-stationary interfaces. The overall layout is shown in Figure~\ref{fig:domain_layout}, and the regions are listed in Table~\ref{tab:domains}.

\begin{figure}[h!]
  \centering
  \fbox{\includegraphics[width=0.95\textwidth]{Figures/2D_drawing/2d_drawing_full_domain.png}}
  \caption[Computational domain]{Computational domain with main dimensions. Only the GV section changes between the design cases.}
  \label{fig:domain_layout}
\end{figure}
 
\begin{table}[h!]
  \centering
  \caption[Domain regions]{Regions of the multi-domain CFX setup, in flow direction.}
  \label{tab:domains}
  \small
  \begin{tabular}{c|l|l}
    \toprule
    \textbf{Region} & \textbf{Description} & \textbf{Mesh tool} \\
    \midrule
    1 & Inlet pipe, $D = 0.5$~m, length $0.5$~m   & ANSYS Meshing (unstructured) \\
    2 & RDT rotor, $Z_r = 7$ blades              & ANSYS TurboGrid (structured) \\
    3 & Guide-vane row (case-dependent)          & ANSYS TurboGrid (structured) \\
    4 & Inner-pipe section, $r < 0.12$~m         & ANSYS Meshing (structured hub) \\
    5 & Conical contraction, $D = 0.5 \to 0.4$~m & ANSYS Meshing (unstructured) \\
    6 & Draft tube: $90^\circ$ bend + final straight pipe ($\sim 1.0$~m) & ANSYS Meshing (unstructured) \\
    \bottomrule
  \end{tabular}
\end{table}
 
\paragraph{Reasons for the extended draft-tube length.}
The final straight pipe in region 6 is approximately $1.0$~m long. It replaces the RPT, for the reasons set out in Section~\ref{sec:rpt-decoupling-justification}, and creates a buffer between the bend exit and the imposed outlet boundary condition. Without this buffer, the imposed static-pressure outlet would project upstream and contaminate the post-bend flow field that is used to evaluate the residual swirl and the post-bend uniformity. With a $\sim 1.0$~m straight buffer, the flow has about $2.5$ duct diameters to redevelop after the bend before it reaches the outlet plane. In swirling internal flows the influence of a static-pressure outlet condition is confined to roughly the last one to two diameters upstream of the outlet, so a buffer of this length keeps the upstream analysis planes effectively free of outlet-induced contamination~\cite{Xia1997}.


\subsection{Axial spacing between the RDT and the guide-vane leading edge}
\label{sec:methods_axial_spacing}

The axial position of the guide-vane leading edge relative to the RDT is set by the geometry inherited from the parallel RDT design~\cite{DjupeslandTrivedi2025} and from the existing draft-tube layout. The total axial distance from the RDT trailing edge to the GV leading edge is $a = 0.192$~m, illustrated in Figure~\ref{fig:rdt_gv_axial_spacing}. A small offset of approximately $30$~mm was introduced between the rotating RDT-GV interface and the GV leading edge. The reason is numerical, not hydraulic. A stationary GV leading edge placed exactly on the rotating-stationary interface generates poor mesh quality at the interface and an unphysical pressure spike on the GV leading edge in the steady frozen-rotor formulation. The offset moves the GV leading edge clear of the interface and removes both effects. The offset was held fixed across all 18 configurations and the \texttt{NoGV} reference, so any effect of $a$ is absorbed uniformly into the comparison and does not bias the relative ranking.

\begin{figure}[h!]
  \centering
  \fbox{\includegraphics[width=0.95\textwidth]{Figures/2D_drawing/RDT-GV drawing.png}}
  \caption[Axial spacing between RDT and GV row]{Axial spacing between the RDT trailing edge and the guide-vane leading edge in the present configuration.}
  \label{fig:rdt_gv_axial_spacing}
\end{figure}

\paragraph{Comparison with the G\"ulich compact-stage bound.}
The axial spacing is normalised as \(a/L\), following the compact-stage criterion from G\"ulich~\cite{Gulich2010}, as discussed in Section~\ref{sec:rsi-axial-spacing}. Here, \(a\) is the axial distance from the RDT trailing edge to the GV leading edge, and \(L\) is the local RDT blade chord. Using the midspan chord, \(L_{\mathrm{mid}}=138.5\)~mm at \(r=0.150\)~m, gives
\begin{equation}
    \frac{a}{L_{\mathrm{mid}}}=\frac{0.192}{0.139}=1.39 .
    \label{eq:my_a_over_L}
\end{equation}
The present spacing is therefore about \(9\) times larger than the upper bound of the compact-stage range \(a/L=0.05\)--\(0.15\). The G\"ulich range is therefore used as a qualitative reference for rotor-stator interaction strength, not as a design constraint that the present geometry is expected to satisfy.

\paragraph{Reduced potential interaction.}
The pressure field attached to each rotor blade decays approximately exponentially with axial distance from the rotor trailing edge~\cite{Korakianitis1989,Hodson1989}. With $a/L_{\mathrm{mid}} = 1.39$, the potential-interaction component of the rotor-stator pulsation field reaching the GV leading edge is expected to be significantly weaker than in a compact stage. Two studies quantify the scaling. Rodriguez et al.~\cite{Rodriguez2007} show that RSI amplitudes in pump-turbines depend on blade counts, operating condition and rotor-stator spacing. Shi et al.~\cite{Shi2022GapMatching} show that axial-clearance matching changes the pressure-pulsation level and dominant-frequency amplitudes in a multiphase pump. This supports the qualitative interpretation used here: the GV row is intended primarily as a swirl-recovery element, not as a tightly coupled stator stage of the RDT.

\paragraph{Persistent viscous interaction.}
The viscous (wake) component is less attenuated by axial spacing. The RDT wake convects downstream through the gap, and the velocity-deficit and azimuthal-non-uniformity content reaches the GV leading edge largely intact~\cite{Korakianitis1989,Duan2019}. The transient simulations in Section~\ref{sec:monitor-setup} are interpreted in this light. The rotor-stator interaction signature in the spectra at $f_{\mathrm{BPF}}$ is expected to be dominated by viscous wake-chopping rather than potential interaction, with corresponding implications for the ring-coherence diagnostic in Section~\ref{sec:spec-coherence}.

A focused follow-up study that varies $a$ explicitly is identified as future work in Chapter~\ref{chap:future_work}.

\subsection{Boundary conditions}
\label{sec:methods_bc}
 
The boundary conditions used in all 18 configurations, in the \texttt{NoGV} reference and in the two transient diagnostic runs are summarised in Table~\ref{tab:bc_master}.
 
\begin{table}[h!]
  \centering
  \caption[Boundary conditions]{Boundary conditions used in the 18 steady simulations and the \texttt{NoGV} reference.}
  \label{tab:bc_master}
  \small
  \begin{tabular}{p{0.30\linewidth} p{0.62\linewidth}}
    \toprule
    \textbf{Boundary} & \textbf{Condition} \\
    \midrule
    Inlet
        & Total pressure $p_{\mathrm{tot}} = 101\,325$~Pa (1~atm), \\
    Outlet
        & Mass flow rate $\dot m = 290.3$~kg/s. \\
    Walls
        & No-slip, automatic wall treatment in the SST formulation. \\
    Reference pressure
        & $p_{\mathrm{ref}} = 0$~Pa (absolute pressure used throughout). \\
    Working fluid
        & Liquid water at $25^\circ$C, default ANSYS material properties. \\
    Rotor speed (RDT, region 2)
        & $n = 650$~rpm, axis aligned with the duct centreline z. \\
    \midrule
    \multicolumn{2}{c}{\textbf{Interfaces}} \\
    \midrule
    Region 1 - Region 2
        & Stationary-rotating, frozen rotor (steady) or transient rotor-stator (transient run). \\
    Region 2 - Region 3
        & Rotating-stationary, frozen rotor (steady) or transient rotor-stator (transient run). \\
    Region 3 - Region 4
        & Stationary-stationary, GGI. \\
    Region 4 - Region 5
        & Stationary-stationary, GGI. \\
    Region 5 - Region 6
        & Stationary-stationary, GGI. \\
    \bottomrule
  \end{tabular}
\end{table}
 
\paragraph{Rotational speed.}
The rotational speed $n = 650$~rpm is the design speed of the RDT booster in the parallel Store2Hydro work~\cite{DjupeslandTrivedi2025}. It gives the rotor frequency $f_n = 10.83$~Hz and the blade-passing frequency $f_{\mathrm{BPF}} = Z_r \cdot f_n = 75.83$~Hz used in the transient analysis (Section~\ref{sec:rsi-thesis}). All 18 steady configurations and the two transient runs are simulated at this speed. Off-design behaviour is not investigated and is identified as future work in Chapter~\ref{chap:future_work}.
 
% ---------------------------------------------------------------------
\section{Mesh strategy and grid convergence}
\label{sec:methods_mesh}

The mesh strategy is region-specific. The rotating RDT and the guide vanes are meshed in ANSYS~TurboGrid, which produces structured hexahedral O-grids with controlled near-wall spacing, low skewness and good orthogonality on blade rows. The inlet pipe, the conical contraction, the $90^\circ$ bend and the final straight pipe are meshed in ANSYS~Meshing with unstructured tetrahedral elements and prismatic inflation layers. Discretisation uncertainty is assessed in two tiers: bounded mesh-quality metrics on the structured TurboGrid regions, and a triplet-based Grid Convergence Index on the unstructured draft-tube region following Celik et al.~\cite{Celik2008}.

\subsection{Rotor and guide-vane meshes (TurboGrid)}
\label{sec:turbogrid_quality}

Each of the 18 guide-vane configurations in Table~\ref{tab:design_matrix} is meshed independently in TurboGrid, since chord and solidity differ between cases. The RDT rotor mesh uses the same workflow with curve files from the upstream RDT design~\cite{DjupeslandTrivedi2025}. The GV mesh uses a target element size of $0.006$~m. The inner-pipe block (region 4, $r < 0.12$~m) is meshed with structured-hub topology, with first-layer thickness matched to the TurboGrid mesh across the shared GGI interface at $r = 0.12$~m, so the cell size stays continuous there. Each blade passage has $5\times10^{5}$ to $1.5\times10^{6}$ elements depending on chord. Figure~\ref{fig:turbogrid_mesh} shows a representative CC and CS mesh.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Turbogrid_mesh2_CC.png}
    \caption{CC, view from hub.}
    \label{fig:turbogrid_cc_2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Turbogrid_mesh3_CC.png}
    \caption{CC, view from shroud.}
    \label{fig:turbogrid_cc_3}
  \end{subfigure}
  \vspace{0.6em}
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Turbogrid_mesh2_CS.png}
    \caption{CS, view from hub.}
    \label{fig:turbogrid_cs_2}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Turbogrid_mesh3_CS.png}
    \caption{CS, view from shroud.}
    \label{fig:turbogrid_cs_3}
  \end{subfigure}
  \caption[GV TurboGrid mesh, CC and CS]{Representative TurboGrid mesh for one CC configuration (top) and one CS configuration (bottom).}
  \label{fig:turbogrid_mesh}
\end{figure}

Face angle, expansion ratio and determinant are bounded on both the RDT and GV meshes and satisfy the TurboGrid limits. Some CS configurations produce local cells with orthogonality angles below the $20^\circ$ limit, down to about $17^\circ$. These sit where the leading edge moves axially along the span, which is intrinsic to the constant-solidity construction. They make up roughly $0.004\,\%$ of the cell count, so the effect on the integral quantities is negligible and the meshes are retained.

A separate GCI triplet is not run for each blade row. TurboGrid generates a fresh structured topology for every case, so one refinement triplet does not transfer across the 18 configurations, and repeating it 18 times is not practical. The blade-row discretisation uncertainty is therefore assessed through two indirect arguments rather than a full GCI. The face angle, expansion ratio and determinant are bounded on every blade mesh and satisfy the TurboGrid limits, so no single case carries an outlier cell quality. Each blade passage is resolved with $5\times10^{5}$ to $1.5\times10^{6}$ elements, a substantial resolution for one stationary vane passage at this Reynolds number. The O-grid topology and the near-wall spacing follow the same construction in all 18 cases, so any residual blade-row discretisation error acts in the same direction for every configuration and is absorbed into the relative comparison rather than biasing it. 
%Dear Håvard: jeg fjernet \cite{Menter1994} her fordi den var feilsitert for mesh-uavhengighet (den er SST-modellartikkelen). Selve bib-oppforingen beholdes, den brukes fortsatt i sec:theory_turbulence. Vil du ha en kilde her, siter en turbomaskin-verifikasjonsstudie, f.eks. Trivedi (Verification_of_Hydraulic_turbines_Chirag_2019) hvis den ligger i bibliography.bib.

\subsection{Inflation layers and \texorpdfstring{$y^+$}{y+} targets}
\label{sec:methods_inflation}

The unstructured regions use 15 inflation layers with a growth ratio of about $1.2$. A fixed $y^+$ target was not enforced anywhere in the domain. Full near-wall resolution, with a $y^+$ of order one on every RDT, GV, cone, bend and pipe wall across all 18 configurations, would push the element count beyond the computational budget available for the campaign. The SST formulation uses automatic wall treatment, which blends between low-Reynolds resolution and a wall function with the local near-wall spacing, so the solver remains usable across the range of $y^+$ values that the screening mesh produces. The near-wall spacing is therefore treated as a modelling limitation rather than a quality target, and a wall-resolved study of the shortlisted candidates with documented $y^+$ is identified as future work in Chapter~\ref{chap:future_work}. Mesh-quality targets and assessment methods are summarised in Table~\ref{tab:mesh_quality}.
%DEAR HÅVARD 10.06: y+ nedtonet i tråd med Ch4. Denne prosaen sa allerede "no fixed y+ target" + automatic wall treatment, så ingen "<=1"/"30-80"-targets gjensto her; jeg la bare til "y+ of order one" for å si eksplisitt hva idealet ville vært, og fjernet det utdaterte Y+-FLAGG-notatet (motsetningen det advarte om var alt borte). tab:mesh_quality sier også "no fixed y+ target", så den står uendret.

\begin{table}[h!]
\centering
\caption[Mesh-quality summary]{Mesh-quality targets and assessment method for the RDT, GV and draft-tube meshes.}
\label{tab:mesh_quality}
\small
\begin{tabular}{l c c c}
\toprule
                            & RDT rotor       & GV row                                   & Draft tube \\
                            & (TurboGrid)     & (TurboGrid)                              & (unstructured) \\
\midrule
Element type                & Hex (O-grid)    & Hex (O-grid)                             & Tet + prism \\
$y^+$ treatment             & \multicolumn{3}{c}{Automatic wall treatment, no fixed $y^+$ target} \\
Min face angle              & $\geq 20^\circ$ & $\geq 20^\circ$ (CS: $17^\circ$ locally) & - \\
Max expansion ratio         & $\leq 1.4$     & $\leq 1.4$                               & - \\
Min determinant             & $\geq 0.4$     & $\geq 0.4$                               & - \\
Inflation layers            & -             & -                                      & 15, growth $\sim 1.2$ \\
Discretisation assessment   & Bounded metrics & Bounded metrics                          & GCI (Sec.~\ref{sec:gci_drafttube}) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Draft-tube mesh and grid convergence}
\label{sec:gci_drafttube}

The draft-tube domain (regions 5 and 6) holds the conical contraction, the $90^\circ$ bend and the final straight pipe. Mesh resolution matters most here. The bend sets up secondary flow with steep cross-stream gradients, and the post-bend profile drives the residual swirl at \texttt{ConeEnd} and the post-bend head loss. A Grid Convergence Index study is run on this domain following Celik et al.~\cite{Celik2008}, with safety factor $F_s = 1.25$ and an acceptance target of $\mathrm{GCI}_{12} \leq 2\,\%$ on every ranking metric. The medium-to-fine band is

\begin{equation}
\mathrm{GCI}_{12} = F_s\,\frac{|\phi_2 - \phi_1|}{|\phi_1|\,(r_{21}^{\,p}-1)}\times 100\,\%,
\label{eq:GCI_def}
\end{equation}

with the apparent order $p$ and the Richardson extrapolation taken from the same reference. The coarse-to-medium band $\mathrm{GCI}_{23}$ uses the same form. The three meshes share a common topology and are refined uniformly, $h_i = (1/N_i)^{1/3}$. Cell counts, element sizes and refinement ratios are given in Table~\ref{tab:gci_meshes} and the cross-sections in Figure~\ref{fig:gci_meshes}.

\begin{table}[h!]
  \centering
  \caption[Mesh triplet for the GCI study]{Cell counts, global element size, inflation first-layer height and refinement ratios for the GCI study on the draft-tube domain.}
  \label{tab:gci_meshes}
  \small
  \begin{tabular}{l r r r r}
    \toprule
    \textbf{Mesh}  & \textbf{Cell count $N$} & \textbf{Element size [m]} & \textbf{First layer [m]} & \textbf{Refinement ratio} \\
    \midrule
    Coarse ($N_3$) & $2\,313\,100$  & $0.0082$ & $0.00067$ & $r_{32} = 1.289$ \\
    Medium ($N_2$) & $4\,956\,300$  & $0.0060$ & $0.00050$ & $r_{21} = 1.305$ \\
    Fine   ($N_1$) & $11\,003\,000$ & $0.0044$ & $0.00037$ & - \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Coarse_CS.png}
    \caption{Coarse, $N_3 = 2.31\times 10^6$ cells.}
    \label{fig:gci_mesh_coarse}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Medium_CS.png}
    \caption{Medium, $N_2 = 4.96\times 10^6$ cells.}
    \label{fig:gci_mesh_medium}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{Figures/Mesh/Fine_CS.png}
    \caption{Fine, $N_1 = 11.00\times 10^6$ cells.}
    \label{fig:gci_mesh_fine}
  \end{subfigure}
  \caption[Draft-tube meshes used in the GCI study]{Cross-sections of the three draft-tube meshes. The base topology is preserved across the triplet so that the refinement is uniform in all three spatial dimensions.}
  \label{fig:gci_meshes}
\end{figure}

The study uses the swirling-flow reference, with the RDT active and the guide-vane region replaced by an empty pipe (the \texttt{NoGV} configuration). It was run on the original RDT mesh, before the hub-blade interface correction documented in Section~\ref{sec:rdt-mesh-overlap}. The correction does not affect the convergence conclusion drawn here. The RDT mesh is held fixed across the three draft-tube meshes, so it does not enter the refinement triplet, and the two quantities flagged below sit outside the affected hub region. The absolute values in Table~\ref{tab:gci_swirl_keep} therefore belong to the original mesh, but the draft-tube mesh convergence that the study tests is unchanged.

On the medium mesh every ranking metric is well inside the target, with $\mathrm{GCI}_{12}$ between $0.05\,\%$ and $0.6\,\%$. The coarse mesh also stays within the $2\,\%$ target on these metrics, with two exceptions. Both lie in the regions with the steepest cross-stream gradients and are listed in Table~\ref{tab:gci_swirl_keep}.

\begin{table}[h!]
  \centering
  \caption[GCI: swirl-dependent quantities]{The two quantities where the coarse-to-medium error exceeds the $2\,\%$ target while the medium-to-fine error does not. Both sit in the steepest-gradient regions: the swirling strength upstream of the guide vanes, and the design-span swirl number on the first plane downstream of the vane region.}
  \label{tab:gci_swirl_keep}
  \small
  \begin{tabular}{l r r r r r}
    \toprule
    \textbf{Quantity} & \textbf{Fine} & \textbf{Medium} & \textbf{Coarse} & $\mathbf{GCI_{12}}$ & $\mathbf{GCI_{23}}$ \\
    \midrule
    Swirling strength upstream of GV $[\mathrm{s}^{-1}]$ & $32.952$ & $32.484$ & $33.645$ & $1.14\,\%$ & $\mathbf{3.08\,\%}$ \\
    Swirl number $S$ downstream of GV (design-span)      & $2.420$  & $2.412$  & $2.422$  & $1.61\,\%$ & $\mathbf{2.12\,\%}$ \\
    \bottomrule
  \end{tabular}
\end{table}

For these two quantities the medium mesh has reached the asymptotic range with respect to the fine mesh, while the coarse mesh has not reached it with respect to the medium mesh. The first is the near-wake of the rotating RDT. The second is the first plane behind the vane region. The largest mesh sensitivity is physically expected there.

On accuracy alone the coarse mesh would have met the target for the ranking metrics. The medium mesh is still selected, mainly for the interface. Its $0.006$~m element size matches the TurboGrid GV target, so the cell size stays continuous across the GGI interface between the draft tube and the vane row. The coarse mesh ($0.0082$~m) is larger than the TurboGrid side and the fine mesh ($0.0044$~m) is smaller, so both introduce a cell-size jump there. The medium mesh also removes the small coarse-to-medium error on the two swirl quantities, and the fine mesh gives almost identical values at $2.22\times$ the cell count. The medium mesh of $4.96\times10^{6}$ cells is therefore used for the RDT, draft-tube and downstream-duct regions in the steady and transient runs.
 %%%%%%%%

% ---------------------------------------------------------------------
\section{Solver settings and convergence}
\label{sec:methods_solver}
 
ANSYS~CFX is used as the solver throughout. The numerical settings adopted for the 18 steady simulations are summarised in Table~\ref{tab:cfx_settings_master}.
 
\begin{table}[h!]
  \centering
  \caption[Solver settings]{Numerical settings used in the 18 steady-state simulations of the design matrix.}
  \label{tab:cfx_settings_master}
  \small
  \begin{tabular}{l l}
    \toprule
    \textbf{Setting} & \textbf{Value} \\
    \midrule
    Solver                       & ANSYS CFX, steady RANS, incompressible \\
    Turbulence model             & SST $k$-$\omega$ \\
    Advection scheme             & High Resolution \\
    Turbulence numerics          & First Order\\
    Timescale control            & Auto (physical timescale derived from rotor speed) \\
    Convergence target           & RMS residuals $\leq 10^{-5}$ for momentum and continuity \\
    Mass-imbalance target        & $|\dot m_{\mathrm{out}} - \dot m_{\mathrm{in}}|/\dot m_{\mathrm{in}} \leq 0.5\,\%$ \\
    Monitor stability            & Drift in monitor metrics $\leq 0.5\,\%$ over the last 200 iterations \\
    \bottomrule
  \end{tabular}
\end{table}
 
\paragraph{Operational notes.}
The same convergence criteria are applied to all 18 steady simulations. The advection terms use the bounded High Resolution scheme, while the turbulence-transport equations use the first-order numerics that are the CFX default. The first-order turbulence treatment adds robustness on the highly loaded passages at the cost of some numerical diffusion in the turbulence fields. This is acceptable for a comparative screening of mean-flow quantities, and the same scheme is applied to every case, so it does not bias the relative ranking. The results are interpreted relative to the \texttt{NoGV} reference, so that the design ranking remains meaningful even when the absolute swirl reduction depends on the downstream draft-tube geometry.
 
% ---------------------------------------------------------------------
\section{Analysis planes and post-processing metrics}
\label{sec:methods_planes_metrics}

A common set of analysis planes is used for all 18 GV configurations and for the \texttt{NoGV} reference. The planes are listed in Table~\ref{tab:planes}. Axial planes from \texttt{GVStart} to \texttt{BendStart} are normal to the streamwise $z$ direction, the bend diagnostic planes \texttt{Bend00}--\texttt{Bend60} follow the local bend axis, and \texttt{Bend90}, \texttt{BendEnd} and \texttt{ConeEnd} are normal to the vertical $y$ direction.

\begin{table}[h!]
  \centering
  \caption[Analysis planes]{Post-processing planes used for GV, draft-tube and bend diagnostics. \texttt{GVEnd\_z680} is the fixed cross-case GV outlet reference plane.}
  \label{tab:planes}
  \small
  \begin{tabular}{l c c c c l}
    \toprule
    \textbf{Plane} &
    \textbf{\(z_{\mathrm{loc}}\) [m]} &
    \textbf{\(z_{\mathrm{global}}\) [m]} &
    \textbf{\(y\) [m]} &
    \textbf{Normal} &
    \textbf{Use} \\
    \midrule
    \texttt{GVStart}       & 0.000 & -0.185 & --    & \(z\) & Start of GV domain; upstream reference \\
    \texttt{GVLE}          & 0.030 & -0.215 & --    & --    & Approximate leading-edge location \\
    \texttt{GVEnd}         & case-dependent & case-dependent & -- & \(z\) & Physical vane trailing-edge plane \\
    \texttt{GVEnd\_z680}   & 0.495 & -0.680 & --    & \(z\) & Fixed GV-passage outlet reference \\
    \texttt{GVEnd2}        & 0.710 & -0.895 & --    & \(z\) & Downstream draft-tube station \\
    \texttt{GVEnd3}        & 0.910 & -1.095 & --    & \(z\) & Downstream draft-tube station \\
    \texttt{GVEnd4}        & 1.110 & -1.295 & --    & \(z\) & Downstream draft-tube station \\
    \texttt{GVEnd5}        & 1.310 & -1.495 & --    & \(z\) & Downstream draft-tube station \\
    \texttt{GVEnd6}        & 1.510 & -1.695 & --    & \(z\) & Downstream draft-tube station \\
    \texttt{BendStart}     & 2.000 & -2.185 & --    & \(z\) & Bend inlet \\
    \texttt{Bend00}        & 2.000 & -2.185 & --    & local & Bend inlet diagnostic plane \\
    \texttt{Bend30}        & --    & -2.434 & 0.067 & local & \(30^\circ\) bend diagnostic plane \\
    \texttt{Bend45}        & --    & -2.536 & 0.146 & local & \(45^\circ\) bend diagnostic plane \\
    \texttt{Bend60}        & --    & -2.615 & 0.249 & local & \(60^\circ\) bend diagnostic plane \\
    \texttt{Bend90}        & --    & -2.682 & 0.497 & \(y\) & Bend-exit diagnostic plane \\
    \texttt{BendEnd}       & --    & --     & 0.497 & \(y\) & Bend exit; head-loss interval endpoint \\
    \texttt{ConeEnd}       & --    & --     & 0.757 & \(y\) & Final downstream plane before RPT location \\
    \bottomrule
  \end{tabular}
\end{table}
 
On each plane, the following metrics are extracted using mass-flow-averaged or area-averaged operators in CFX-Post:
\begin{itemize}
  \item static and total pressure $\overline{p}$, $\overline{p_t}$, both mass-flow-averaged and area-averaged;
  \item total head $H = \overline{p_t}/(\rho g)$;
  \item swirl number $S$ (Eq.~\ref{eq:S_def}) and its design-span-restricted version $S_{\mathrm{span}}$ over the design span $r \in [0.12, 0.25]$;
  \item area-averaged swirling strength $\overline{\lambda_{ci}}$;
  \item turbulent kinetic energy $\overline{\mathrm{TKE}}$ and its plane maximum;
  \item mass-flow-averaged flow angle $\overline{\alpha}^{m}$.
\end{itemize}

The metrics are not all used for the same purpose. \(\Delta H_{\mathrm{sys}}\) and \(|\alpha|_{\mathrm{ConeEnd}}\) define the Pareto screening. \(\mathrm{HL_{total}}\), \(S\), \(S_{\mathrm{span}}\), \(\lambda_{ci}\) and \(Q\) are used to explain where the hydraulic cost and residual rotation originate.
Mass-flow conservation between consecutive planes is checked separately as a sanity test. Forces and torques on the rotor and on a representative guide vane are extracted in addition.
 
\paragraph{Ranking.}
The final screening ranking is based on the Pareto-front analysis between the system head increase \(\Delta H_{\mathrm{sys}}\) and the mass-flow-averaged residual flow angle \(|\alpha|_{\mathrm{ConeEnd}}\). The total head loss \(\mathrm{HL_{total}}\) is reported as a supporting cost metric, while the swirl number \(S\) is used to interpret angular-momentum transport and streamwise swirl development.
 
% ---------------------------------------------------------------------
\section{No-GV reference simulation}
\label{sec:methods_rdt_baseline}
 
A steady reference simulation of the same multi-domain CFX layout with the guide vanes \emph{removed}, denoted \texttt{NoGV} in Table~\ref{tab:design_matrix}, is performed to provide the baseline against which all 18 configurations are compared. In this reference, region 3 is replaced by an empty straight pipe of equal length while every other aspect of the domain, the boundary conditions and the solver settings is held identical. The simulation produces the no-GV residual swirl in the draft-tube section, which is used to normalise the swirl-reduction metric:
\begin{equation}
\mathcal{R}_S(\mathrm{plane}) \;=\; \frac{S_{\mathrm{NoGV}}(\mathrm{plane}) - S_{\mathrm{case}}(\mathrm{plane})}{S_{\mathrm{NoGV}}(\mathrm{plane})}.
\label{eq:swirl_reduction}
\end{equation}
The \texttt{NoGV} reference is also used to verify that the RDT model produces the expected wake structure (high swirl concentrated in the outer span, low-momentum or back-flow region in the centre, swirling-strength concentrated in the tip-vortex helix). Visual inspection of the reference confirms the qualitatively correct behaviour, which gives confidence that the inflow to the guide-vane region is dominated by the RDT in the way that the project work~\cite{Leonsen2025} designed for. In addition, comparison with \texttt{NoGV} separates the hydraulic effect of adding guide vanes from the untreated RDT-draft-tube response. In Chapter~\ref{chap:results_discussion}, this reference is used for residual-swirl interpretation, bend-loss comparison and the system-head reference, while the guide-vane and downstream losses are reported through the separate head-loss decomposition.

\paragraph{Distinction from the design-input simulation.}
The steady \texttt{NoGV} reference described here is a separate run from the stand-alone transient RDT simulation that produces $\alpha_{\mathrm{base}}(r)$ (Section~\ref{sec:methods_design_input_baseline}). The two share the same RDT geometry and the same operating point. The \texttt{NoGV} reference is steady and is the swirl-reduction baseline used by all 18 simulations of the design matrix. The stand-alone transient RDT simulation is transient and produces a time-averaged wake field used only as input to the parametric MATLAB tool. The two are not interchangeable.
 
% ---------------------------------------------------------------------
\section{Justification for not coupling with the RPT}
\label{sec:rpt-decoupling-justification}
 
The decision to replace the RPT by a $\sim 1.0$~m extended straight pipe is the most consequential simplification in the present setup. It is justified by three considerations, all of which are specific to the master-thesis context.
 
\paragraph{Computational budget.} A coupled RDT-GV-RPT simulation in CFX requires about $3$-$4$ times more wall-clock time per case than the present RDT-GV-draft-tube simulation. Three factors drive this. The RPT runner adds a rotating-mesh region. The runner blades require smaller blade-passage cell sizes. The rotor-stator interfaces require stricter timescale control for stable convergence. With 18 configurations to simulate, the available computational allocation does not accommodate this scaling.
 
\paragraph{Division of work in the Store2Hydro programme.} The parallel master thesis by Dokset~\cite{Dokset2025} is dedicated to the RPT-side analysis and to the cavitation behaviour of the runner under various pre-rotation conditions imposed at the runner inlet. The work is therefore divided between the two theses. The present thesis covers the guide-vane design space and identifies a small number of candidate configurations that produce favourable inflow conditions at the final downstream plane before the RPT. Dokset's thesis takes selected inflow profiles, including those produced by the present work, and assesses the resulting pump-mode performance and cavitation margin in the RPT. A coupled simulation by either side would duplicate the other side's responsibility.
 
\paragraph{Comparability of the relative ranking.} In pump mode the RPT runner draws fluid through itself, and its suction field shapes the upstream draft-tube velocity profile; this effect is absent when the RPT is replaced by a straight pipe, so the present setup does not predict absolute RPT-side performance. The question addressed here is instead a relative one: which of the 18 configurations is most favourable on the recovery-loss trade-off? The relative ranking is expected to be more robust than the absolute values, to the extent that the missing RPT suction field acts in the same way across the cases. The bulk strength of that suction field is set mainly by the imposed mass flow and the fixed runner geometry, both identical across the 18 configurations, so a first-order uniform effect is a reasonable starting assumption for a first ranking. The assumption is weakest where the inflow presented to the runner differs most between cases. The residual swirl at the downstream plane ranges from co-rotating to counter-rotating across the matrix, and pump-mode studies of this runner report a strongly sign-dependent response to incoming pre-rotation (Section~\ref{sec:sign-of-swirl}). The back-coupling neglected here is therefore unlikely to be exactly uniform across the sign divide. Absolute values of head loss and residual swirl will shift when the RPT is reintroduced, and rankings among cases that differ mainly in the sign of the residual swirl are the part most exposed to that shift. The selected candidate should therefore be verified in a coupled RDT-GV-RPT simulation.
 
A coupled simulation of the selected configuration (the case identified by the Pareto analysis as the recommended candidate) with the RPT included is identified in Chapter~\ref{chap:future_work} as the natural follow-up to this work and is left as future work.
 
% ---------------------------------------------------------------------
\section{Method limitations}
\label{sec:methods_limitations_master}
 
Each modelling choice in this chapter is stated with its limitation where it is introduced: the single design wake and operating point, the steady-RANS turbulence treatment, the indirect cavitation check, the fixed $\delta_{\mathrm{TE}} = 6^\circ$ slip correction, and the replacement of the RPT by an extended pipe. The consequences of these choices for the design ranking are discussed with the results and drawn together in Section~\ref{sec:limitations}.

% ---------------------------------------------------------------------
\section{Transient simulation setup and monitor points}
\label{sec:monitor-setup}
 
Two transient simulations are run in addition to the 18 steady simulations of the design matrix. The first is \texttt{CC\_450\_7}, the project-work baseline with $Z_r=Z_s=7$ and the $m_{\min}=0$ reference. The second is \texttt{CC\_400\_6}, the $m_{\min}\geq 1$ counterpart from the design matrix. The choice of \texttt{CC\_400\_6} rather than \texttt{CC\_450\_6} is motivated in Section~\ref{sec:rsi-thesis}. Both runs are performed in ANSYS~CFX at $n = 650$~rpm to assess the unsteady loading at the nominal operating point and to provide the time histories needed for the spectral analysis. The interface between the rotating RDT domain and the stationary GV domain is specified as a Transient Rotor-Stator (TRS) interface; all other solver settings follow those established for the 18 steady simulations (Section~\ref{sec:methods_solver}).
 
The time step is fixed at $\Delta t = T_{\mathrm{rev}}/360 = 2.564\times 10^{-4}$~s, corresponding to one degree of rotor rotation per time step. The total simulated physical time is $15\,T_{\mathrm{rev}} \approx 1.38$~s, allowing the initial transient to decay over approximately ten rotor revolutions before the last five revolutions are retained for Fourier analysis.
 
\subsection{Coordinate system and reference positions}
\label{sec:coord-system}
 
The coordinate system used throughout the spectral analysis has its origin at the geometric centre of the RDT hub. Axial monitor positions are reported as the distance $s$ measured in the streamwise direction from the RDT-GV interface towards the GV outlet. The RDT-GV interface plane is located at
\begin{equation}
    z_{\mathrm{ref}} = -0.185~\mathrm{m}.
    \label{eq:zref}
\end{equation}
The duct radius is $R = 0.25$~m, consistent with the baseline geometry established in~\cite{Leonsen2025}. Downstream positions are then reported as
\begin{equation}
    s = |z| - |z_{\mathrm{ref}}|,
    \qquad 0~\mathrm{mm} \leq s \leq 500~\mathrm{mm}.
\end{equation}
This convention is used only to present monitor locations along the GV passage; velocity signs are handled separately in the post-processing convention. Downstream of $z_{\mathrm{ref}}$ there is a short unloaded transition before the GV leading edge, and the GV domain extends to $z = -0.685$~m, giving a total GV domain length of 500~mm.
 
\subsection{Monitor-signal indexing}
\label{sec:monitor-index}
 
Seven monitor signals are exported from the CFX solver manager and used in the spectral analysis. To keep figure axis labels and table rows compact, each signal is assigned a short label in Table~\ref{tab:monitor-index}. These short labels are used on all figures in Chapter~\ref{chap:results_discussion} and in Appendix~\ref{app:individual_spectra}; their full meaning is to be read from Table~\ref{tab:monitor-index}.
 
\begin{table}[H]
\centering
\small
\caption[FFT monitor signals]{Monitor signals used in the FFT analysis. Short labels are used in the plots.}
\label{tab:monitor-index}
\begin{tabular}{p{0.12\textwidth} p{0.20\textwidth} p{0.36\textwidth} p{0.24\textwidth}}
\hline
Label & Quantity & Location / averaging & CFX expression \\ \hline
\multicolumn{4}{l}{\textit{Group A - force on a single guide vane (GV1)}} \\
$F_{\mathrm{res}}$ & Resultant force & Integrated on GV1 surface & \texttt{Fres GV1} \\[0.4em]

\multicolumn{4}{l}{\textit{Group B - torque on the rotor}} \\
$M_{\mathrm{RDT}}$ & Torque about rotation axis & Integrated over full RDT & \texttt{torque\_z()@Entire BLADE} \\[0.4em]

\multicolumn{4}{l}{\textit{Group C - plane-averaged pressures}} \\
$P_{\mathrm{in}}$ & Static pressure, mass-flow avg. & Plane at $s = 0$ mm (RDT-GV interface) & \texttt{massFlowAve(p)@RDT\_GV} \\
$P_{\mathrm{out}}$ & Static pressure, mass-flow avg. & Plane at $s = 500$ mm (GV outlet) & \texttt{massFlowAve(p)@GV\_DT} \\[0.4em]

\multicolumn{4}{l}{\textit{Group D - wall-pressure probes at $r/R = 0.744$ along the GV passage}} \\
$P_0$ & Static pressure & $s = 0$ mm, $r/R = 0.744$ (near GV leading edge) & \texttt{Static Pressure point} \\
$P_3$ & Static pressure & $s = 300$ mm, $r/R = 0.744$ (inside GV passage) & \texttt{Static Pressure point} \\
$P_5$ & Static pressure & $s = 500$ mm, $r/R = 0.744$ (at GV outlet) & \texttt{Static Pressure point} \\ \hline
\end{tabular}
\end{table}
 
\paragraph{Groups of purpose.}
The signals fall into four functional groups:
\begin{itemize}
  \item \emph{Group A - Force on a single guide vane.} The resultant force $F_{\mathrm{res}}$ on a representative guide vane (GV1) is the principal diagnostic for cyclic structural loading.
  \item \emph{Group B - Torque on the rotor.} $M_{\mathrm{RDT}}$ is the torque about the rotation axis integrated over the entire RDT blade set and captures periodic torque ripple from rotor-stator interaction.
  \item \emph{Group C - Plane-averaged pressures.} Two mass-flow-averaged plane signals, $P_{\mathrm{in}}$ at the RDT-GV interface and $P_{\mathrm{out}}$ at the GV outlet, extract the axisymmetric $m=0$ component of the pulsation field. Comparison of $P_{\mathrm{in}}$ and $P_{\mathrm{out}}$ amplitudes quantifies how much of the coherent $m=0$ content is attenuated across the GV row.
  \item \emph{Group D - Wall-pressure probes along the GV passage.} Three probes $P_0$, $P_3$, $P_5$ at the fixed radial position $r/R = 0.744$ along one azimuthal line sample the full pulsation field, including the spinning modes $|m|\geq 1$ that are washed out in the plane-averaged signals. In combination with $P_{\mathrm{out}}$ they permit the ring-coherence diagnostic defined in Eq.~\eqref{eq:coherence}.
\end{itemize}
 
\section{Spectral analysis methodology}
\label{sec:spec}
 
The force, torque and pressure signals from the monitors described in Section~\ref{sec:monitor-index} are analysed in the frequency domain using the discrete Fourier transform. This section summarises the sampling, window and amplitude-calibration relations and defines the ring-coherence diagnostic used to classify the dominant spatial mode of the pulsation field.
 
\subsection{Discrete Fourier transform and sampling}
\label{sec:spec-dft}
 
A monitor signal $x(t)$ is recorded at the transient solver time step $\Delta t$, giving $N$ uniformly spaced samples $x_n = x(n\Delta t)$, $n = 0,1,\dots,N-1$. The sampling frequency and Nyquist frequency are
\begin{equation}
    f_s \;=\; \frac{1}{\Delta t},\qquad
    f_{\mathrm{Nyq}} \;=\; \frac{f_s}{2}. \label{eq:fs_fnyq}
\end{equation}
The discrete Fourier transform and frequency grid are
\begin{align}
    X_k &\;=\; \sum_{n=0}^{N-1} x_n\, e^{-i\,2\pi k n / N},
    \qquad k = 0,1,\dots,N-1, \label{eq:dft}\\
    f_k &\;=\; \frac{k}{N\Delta t} \;=\; k\,\Delta f,
    \qquad \Delta f = \frac{1}{N\,\Delta t} = \frac{f_s}{N}.
    \label{eq:dft_f}
\end{align}
To place the excitation frequencies $f_n$ and $f_{\mathrm{BPF}}$ exactly on Fourier bins and thereby minimise spectral leakage, the analysis window is chosen as an integer number of rotor revolutions,
\begin{equation}
    T_w \;=\; N_{\mathrm{rev}}\,T_{\mathrm{rev}},
    \qquad T_{\mathrm{rev}} = 1/f_n, \label{eq:Tw}
\end{equation}
so that $N = 360\,N_{\mathrm{rev}}$ at one degree of rotor rotation per solver step. With $N_{\mathrm{rev}}=5$ this gives $\Delta f = f_n/5$, i.e.\ five Fourier bins per rotor-frequency harmonic and 35 bins per blade passing harmonic.
 
\subsection{Hann window and leakage correction}
\label{sec:spec-window}

Residual non-periodicity at the boundaries of the analysis window introduces spectral leakage from dominant peaks into neighbouring bins. A Hann window is therefore applied before the Fourier transform, following standard DFT practice for harmonic amplitude estimation~\cite{Harris1978}:
\begin{equation}
    w_n \;=\; \tfrac{1}{2}\!\left[ 1 - \cos\!\left(\tfrac{2\pi n}{N-1}\right)\right],
    \qquad n = 0,1,\dots,N-1 .
    \label{eq:hanning}
\end{equation}
The mean is removed before windowing, \(x'_n = x_n - \bar{x}\), and the windowed signal is \(x^{w}_n = x'_n w_n\). The Hann window reduces the coherent amplitude of a bin-centred sinusoid by the factor \(\sum_n w_n/N \approx 1/2\). The single-sided amplitude spectrum is therefore corrected by the coherent gain of the window:
\begin{equation}
    A_k \;=\; \frac{2}{\displaystyle\sum_{n=0}^{N-1} w_n}\,
      \bigl|X^{w}_k\bigr|,
    \qquad k = 0,1,\dots,\tfrac{N}{2}.
    \label{eq:amplitude}
\end{equation}
The factor \(2\) accounts for folding the negative-frequency content onto the single-sided spectrum. \(A_k\) has the same physical units as \(x_n\) and represents the peak amplitude of a bin-centred sinusoid at frequency \(f_k\).
 
\subsection{Periodicity verification}
\label{sec:spec-periodicity}
 
The Fourier amplitudes $A_k$ are only meaningful if the underlying signal is statistically stationary over the analysis window. Two verifications are therefore performed:
\begin{enumerate}
  \item \emph{Phase-folded comparison.} The $N_{\mathrm{rev}}$ revolutions contained in the window are plotted on a common $[0,\,360^\circ]$ phase axis. A signal dominated by harmonics of $f_n$ produces nearly overlapping curves; deviation is a direct indicator of content at frequencies below $f_n$ or of incomplete transient decay.
  \item \emph{Window robustness test.} The spectrum is computed for $N_{\mathrm{rev}} = 5$ and $N_{\mathrm{rev}} = 6$ and the two spectra are overlaid. Stability of the peak amplitudes under variation of the window length confirms that the reported amplitudes are not an artefact of the particular window.
\end{enumerate}
 
\subsection{Ring-coherence diagnostic}
\label{sec:spec-coherence}
 
The Tyler-Sofrin analysis in Section~\ref{sec:rsi-tyler} distinguishes between the axisymmetric $m=0$ mode and the spinning modes with $|m|\geq 1$. Only the $m=0$ component produces a net axial force on the GV ring and propagates as a plane wave, whereas the spinning modes cancel in circumferential integration.
 
To classify the dominant spatial mode of the computed pulsation field without a full modal decomposition, a scalar ring-coherence diagnostic is defined. Let $A_{\mathrm{loc}}(f)$ denote the Fourier amplitude of a local wall-pressure signal (one of $P_0 \ldots P_5$) and $A_{\mathrm{ring}}(f)$ the amplitude of the plane-averaged signal $P_{\mathrm{out}}$ on the downstream plane. Because mass-flow averaging extracts the $m=0$ component of the field, $A_{\mathrm{ring}}$ measures the axisymmetric plane-wave content alone. The ring-coherence ratio is defined as
\begin{equation}
    \mathcal{R}_m(f) \;=\;
    \frac{A_{\mathrm{ring}}(f)}{A_{\mathrm{loc}}(f)},
    \qquad 0 \leq \mathcal{R}_m(f) \leq 1. \label{eq:coherence}
\end{equation}
Two limiting interpretations follow:
\begin{itemize}
  \item $\mathcal{R}_m(f_{\mathrm{BPF}}) \to 1$ indicates that the pulsation at $f_{\mathrm{BPF}}$ is dominated by the $m=0$ breathing mode. For $Z_r = Z_s$ this is the expected signature of the classical rotor-stator concern, and is the main signal that the \texttt{CC\_450\_7} transient run is designed to test.
  \item $\mathcal{R}_m(f_{\mathrm{BPF}}) \ll 1$ indicates that the pulsation is dominated by spinning modes $|m|\geq 1$ that cancel in circumferential integration. The net axial ring force is then small and the classical $Z_r = Z_s$ concern does not manifest strongly in practice. The \texttt{CC\_400\_6} transient run is the comparator from the design matrix that is expected to display this behaviour.
\end{itemize}
 
\subsection{Characteristic excitation frequencies}
\label{sec:spec-peaks}
 
Table~\ref{tab:characteristic_freqs} lists the characteristic excitation frequencies for the two transient runs at $n = 650$~rpm. Note that $f_{\mathrm{BPF}}$ depends only on the rotor blade count $Z_r=7$ and is therefore the same for both \texttt{CC\_450\_7} and \texttt{CC\_400\_6}, while the vane passing frequency differs ($Z_s=7$ versus $Z_s=6$).
 
\begin{table}[H]
\centering
\caption[Characteristic excitation frequencies]{Characteristic excitation frequencies for the two transient runs ($n = 650$~rpm, $Z_r = 7$).}
\label{tab:characteristic_freqs}
\begin{tabular}{lcccc}
\hline
 & Symbol & Formula & \texttt{CC\_450\_7} [Hz] & \texttt{CC\_400\_6} [Hz] \\ \hline
Rotor frequency           & $f_n$               & $n/60$         & 10.83  & 10.83  \\
Second rotor harmonic     & $2f_n$              & $2n/60$        & 21.67  & 21.67  \\
Third rotor harmonic      & $3f_n$              & $3n/60$        & 32.50  & 32.50  \\
Blade passing frequency   & $f_{\mathrm{BPF}}$  & $Z_r\,f_n$     & 75.83  & 75.83  \\
Vane passing frequency    & $f_{\mathrm{VPF}}$  & $Z_s\,f_n$     & 75.83  & 65.00  \\
Second BPF harmonic       & $2f_{\mathrm{BPF}}$ & $2Z_r\,f_n$    & 151.67 & 151.67 \\
Third BPF harmonic        & $3f_{\mathrm{BPF}}$ & $3Z_r\,f_n$    & 227.50 & 227.50 \\ \hline
\end{tabular}
\end{table}

A characteristic signature of rotor-stator interaction is energy at \(f_{\mathrm{BPF}}\) and its harmonics, because the rotating blade wakes and pressure field pass the stationary vane row once per blade passage~\cite{Brekke2001}. Dominant energy at \(f_n\) and its harmonics is interpreted here as rotor-periodic content rather than blade-passing content. Possible sources are rotor imbalance, blade-to-blade loading variation, or azimuthally non-uniform inflow into the RDT. Both types of content are analysed in Chapter~\ref{chap:results_discussion}.

% ---------------------------------------------------------------------
\clearpage